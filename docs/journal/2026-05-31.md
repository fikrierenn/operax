# Oturum Günlüğü — 2026-05-31

## Ana Konu
F0 güvenlik zemini tamamlama + kod-review borçlarının kapatılması + doküman toparlama. Önceki oturumun SEC-1 force-push'u tamamlandı; F0.2 production güvenlik (plan 17) DB-driven RBAC modeliyle baştan tasarlanıp uygulandı; F0.1 kod-review borçları (CRIT/HIGH/IMP) doğrulanıp kapatıldı; doküman dağınıklığı klasör yapısına oturtuldu.

## Tamamlananlar
- **SEC-1 git scrub force-push** — origin/main `9672a44→7b8ce88`, feat `4931033→9866cd0`; remote'taki eski şifreli geçmiş ezildi.
- **F0.2 — Production Güvenlik (plan 17)** build 0/0, migrate+seed DB doğrulandı:
  - Rate Limiting (login 10/dk-IP `[EnableRateLimiting]`, switch-company 10/dk-kullanıcı, global 200/dk)
  - Security Headers (NetEscapades): CSP/XFO/nosniff/XSS/Referrer/Permissions
  - Serilog: IP/UserAgent enrich (`WithRequestHeader("User-Agent","ClientAgent")`) + `logs/operax-*.log`
  - Cookie SecurePolicy ortam-koşullu (prod Always/dev SameAsRequest), HSTS 1yr
  - **DB-driven RBAC** (kullanıcı kararıyla hardcoded yerine): `RoleModuleAccess` tablo (`schema_M00_Security.sql`) + `Lib/Authz/` (ModuleKeys/Requirement/Handler, IMemoryCache, Administrator bypass, GET=VIEW/POST=EDIT) + `Program.cs` policy döngüsü + AuthorizeFolder + 7 şablon rol seed (`Roles.cs`, `SeedData`) + default RoleModuleAccess seed (plan §4 ile birebir doğrulandı) + **Admin/Roles → Modül İzinleri UI** (`Permissions.cshtml`)
  - CLI→UserSecrets (`AddUserSecrets("ab29872f-...")` + Json 10.0.8) — SEC-1 sonrası kırılan migrate düzeltildi
- **`sex`→`sqlEx` rename** — 56 kod + 10 kural dosyası + naming kuralı (`csharp-conventions.md`) + memory (`no-ambiguous-abbreviations.md`)
- **F0.1 kod-review borçları KAPANDI** (canlı kod doğrulandı):
  - CRIT-3 magic string → 38 SQL literal `DocStatus` parametresine (14 dosya, 4 paralel ajan) + bonus 2 injection-concat + CRIT-3b Picking
  - CRIT-4 ILogger → 10 dosyada try/catch+LogError'a bağlandı (10→0 uyarı; SalesOrders/Index'e logger eklendi)
  - HIGH-1/HIGH-2/IMP-2/SEC-1/2/3 → stale doğrulandı, kanıtla kapatıldı
  - Türkçe yorum eksiği → eklendi
- **STYLE-1 inline style cleanup** — `parts/_docdetail.css` (yeni) + PO/Details 44→23, SalesInvoices/Details 30→~8, Cheques/Index; tüm color/font/border inline kaldırıldı; preview'da PO+SalesInvoices görsel doğrulandı (regresyon yok)
- **Doküman toparlama** — `docs/` 22 md → canonical(10)+`design/`+`reference/`+`archive/`; `INDEX.md` haritası; cross-ref path'leri (docs + .claude) düzeltildi; `MASTER_EXECUTION_PLAN` M-F0.1 ✅ işaretlendi; TODO 119-159 mükerrer blok SUPERSEDED

## Build / Test Durumu
- Build: **yeşil 0/0** (STYLE-1 sonrası doğrulandı; sonrasında sadece doküman taşıması — kod değişmedi)
- Test: otomatik test yok (proje geneli durum)
- Smoke test: PO/Details + SalesInvoices/Details preview'da görsel doğrulandı; RBAC seed DB sorgusuyla doğrulandı (7 rol + erişim sayıları plan §4 ile birebir)

## Commit Durumu
- Uncommitted: **81 dosya** (commit-discipline 15 eşiği AŞILDI — bir sonraki iş öncesi commit-split ŞART)
- Yeni commit: yok (kullanıcı "commit atma" dedi)
- Beklemede: tüm oturum işi (F0.2 + sex rename + CRIT-3/4 + STYLE-1 + doküman reorg)

## Aktif Planlar
- `plans/17-production-security-hardening.md` — TAMAM (kod+DB+doğrulama); runtime smoke (403/429/header) opsiyonel kaldı. Arşive taşınabilir.
- 13 aktif plan duruyor (12 izolasyon, 13 switch-company, 14 ledger sırada)

## Yarım Kalan / Yarın'a Bırakılan İşler
- **COMMIT-SPLIT (81 dosya)** — yeni işe başlamadan önce mantıklı bucket'lara böl: (1) sql/şema (2) Lib/Authz+Roles (3) Program/RBAC+SeedData (4) CRIT-3 param (5) CRIT-4/yorum (6) sex rename (7) STYLE-1 css/view (8) doküman reorg (9) plan/TODO/docs
- **plan 17 runtime smoke** (opsiyonel) — farklı rolle 403, Viewer POST→403, login 429, response header doğrulama
- **SEC-1 sa şifre rotate** (düşük risk borç — lokal+private)
- **TODO.md trim** (1542 satır — superseded/M00-M17 ekran listeleri ayrıştırılabilir; doküman reorg'un ikinci aşaması)

## Kararlar
- **RBAC DB-driven** (kullanıcı 2026-05-31): roller `AspNetRoles`'ta dinamik + `RoleModuleAccess` admin-yönetimli; hardcoded `[Authorize(Roles=)]` REDDEDİLDİ. Plan 17 §3-C.3'e işlendi.
- **Yetki birimi = üst Feature klasörü** (Module katalogu lisans için kalır; folder'larla 1:1 değil).
- **TLS-2 reddedildi** — dev http korundu (prod Always).
- **Yanlış-anlamlı kısaltma yasağı** (`sex`→`sqlEx`) — kural + memory.
- **Doküman: MASTER_EXECUTION_PLAN tek canonical plan**; superseded → `archive/` (silme yok).

## Dikkat Edilmesi Gerekenler
- İki ayrı "F0.2" numarası karışıyor: TODO/plan 17 "F0.2" = production güvenlik (YAPILDI); MASTER_EXECUTION_PLAN "M-F0.2" = Multi-Company İzolasyon (plan 12, YAPILMADI). Sonraki oturum master plan numarasını izler.
- DapperRoleStore `IRoleClaimStore` implement ETMİYOR (RBAC bu yüzden tablo modeli, claim değil).
- Doküman taşıması git mv ile yapıldı (geri alınabilir); cross-ref'ler düzeltildi ama yeni dış referans eklenirse `docs/INDEX.md`'ye bak.

## Yarına Başlangıç Noktası
1. **COMMIT-SPLIT** — 81 dosyayı yukarıdaki 9 bucket'a böl, sırayla commit et (yeni işe başlamadan ZORUNLU).
2. **M-F0.2 Multi-Company İzolasyon (plan 12)** ile başla — MASTER_EXECUTION_PLAN'a göre F0'ın sıradaki modülü (M-F0.1 kapandı). Plan 12 zaten var; CompanyId'siz sorgu sweep + tarama testi.
3. Sonra M-F0.3 (switch-company Model 3, firma-bağlamlı rol — RBAC'ı tamamlar) → M-F0.4 (ledger immutability, plan 14).

---

# Oturum Günlüğü — 2026-05-31 (2. oturum — F0 izolasyon/yetki/ledger maratonu)

## Ana Konu
Commit-split (biriken 67 dosya) + MASTER_EXECUTION_PLAN F0 modülleri sırayla: M-F0.2 (plan 12 izolasyon), M-F0.3 (plan 13 switch-company), M-F0.4 (plan 14 ledger immutability/dönem kontrolü).

## Tamamlananlar
- **Commit-split** — 67 dosya → 9 mantıklı commit (güvenlik şeması, RBAC çekirdek, Program/middleware, rules/docs, magic-string refactor, CSS, RBAC UI, INDEX). Halüsinasyon sebebi: commit-splitter diff okumadan ezbere mesaj yazıyordu → diff-zorunlu talimatla düzeltildi.
- **Plan 12 — İzolasyon guard (TAM):**
  - `src/Operax.Cli/IsolationScanner.cs` + `scan-isolation` komutu (52 doğrudan + 27 dolaylı company-scoped tablo envanteri). CompanyId predikatsız ham sorgu → exit 1.
  - 55 aday → 3 gerçek fix (AutoTraceability, Transfer/Replenishment, Production/Terminal firma guard) + 33 defense-in-depth suppress (kendini-açıklayan yorum) + 19 dead/WIP suppress.
  - Production/Terminal (canlı sayfa) gerçek izolasyon açığı kapatıldı: OnPostStart order firma-guard + UPDATE CompanyId + bare-catch→logger.
  - **scan-isolation = 0 ihlal.** security-principles §8 guard pointer.
- **Plan 13 — switch-company hardening (TAM + runtime doğrulandı):**
  - Canlı VT'de 2 bug bulundu+düzeltildi: (1) UserCompany.IsActive kolonu YOKtu ama sorgu kullanıyordu → switch tamamen kırıktı; (2) UserCompany.Role='Admin' ≠ Roles.* 'Administrator'.
  - `Lib/CompanyAwareClaimsPrincipalFactory.cs` — firma-bağlamlı rol (Model 3): rol claim'i aktif company'nin UserCompany.Role'undan üretilir; switch RefreshSignIn → rol otomatik güncellenir.
  - Endpoint: IAntiforgery doğrulama (CSRF kapatıldı) + COMPANY_SWITCH audit. Sidebar: firma listesi UserCompany'ye filtreli.
  - Runtime smoke: login → 'Administrator' çözüldü, Admin-only sayfa açıldı, factory login'i kırmadı.
- **Plan 14 — Ledger dönem kontrolü (mekanizma ÇEKİRDEĞİ):**
  - ADR-01 (`docs/ADR/01-ledger-clustered-key.md`): clustered key = BIGINT IDENTITY + GUID nonclustered. Canlı VT'de StockMovement/AccountMovement/FinTx PK = random-GUID CLUSTERED teyit (fragmentasyon). Mevcut tablo dönüşümü Faz 2.
  - `docs/sql/schema_M11_LedgerIntegrity.sql`: AccountingPeriod (OPEN/CLOSED/LOCKED) + PeriodOverrideLog (silinmez, ADR BIGINT clustered, CHECK'ler) + sp_GuardPeriodOpen + sp_GuardStockFrozen (no-op kanca).
  - 5 senaryo canlı DB'de doğrulandı: OPEN geçer · CLOSED gerekçesiz→51202 · LOCKED→51201 koşulsuz · yetkisiz→51203 · yetkili→geçer+log.
  - document-immutability.md §1.b kuralı eklendi.

## Build / Test Durumu
- Build: **yeşil** (web 0 hata @ plan 13 son kod değişimi; CLI 0 hata @ plan 14). Plan 14 sonrası web kod değişmedi.
- Test: otomatik test projesi yok (mevcut durum). SQL guard'lar canlı DB'de manuel doğrulandı.
- Smoke test: plan 13 login+rol runtime doğrulandı (preview server).

## Commit Durumu
- Uncommitted: **0 dosya** (temiz).
- Bu oturum ~26 commit (commit-split 11 + plan 12 ~6 + plan 13 ~2 + plan 14 ~2 + ara docs).

## Aktif Planlar
- plans/12-data-isolation-guard.md — **TAMAM** (sprint-kapanış: dead-servis çözülünce guard'ı blocking hook'a bağla).
- plans/13-switch-company-hardening.md — **TAMAM** (kalan smoke: çok-firma switch/CSRF/403 — çok-firma seed gerek).
- plans/14-ledger-pk-immutability.md — mekanizma çekirdeği DONE; entegrasyon KALAN (aşağıda).

## Yarım Kalan / Yarın'a Bırakılan İşler
- **Plan 14 entegrasyon (riskli, birbirine bağlı — temiz kafayla yap):**
  1. AccountMovement IsDeleted kaldır — 2 filtreli index rebuild (IX_AccountMovement_Partner_Date, UX_AccountMovement_Source); referans yok, contained.
  2. StockMovement cancel→reversal SP (`sp_*Reverse` + IsCancelled=1 set; şu an HİÇ set edilmiyor).
  3. Period trigger (emniyet ağı) — ⚠️ override INSERT'i trigger'a takılır → SESSION_CONTEXT('period_override_ok') ile guard↔trigger koordinasyonu gerek.
  4. Post-SP guard enjeksiyonu (Receiving/Shipping/Transfer/CycleCount/ProductionFinish ilk satırına sp_GuardPeriodOpen) — movement date kaynağı + trigger çakışması dikkat. Bu adım guard'ı CANLI yapar.
- **Production dead servisler** (TODO): ProductionReceipt/Activity/DynamicBom — WIP terminal planlanırsa SP'ye taşı, yoksa sil (architecture §4).
- **sa şifre rotate** (düşük-risk borç).

## Kararlar
- **ADR-01 (2026-05-31):** ledger clustered key = BIGINT IDENTITY clustered + GUID nonclustered. Mevcut tablo migration Faz 2.
- **Plan 14 K4 kapsam onayı:** immutability + dönem kontrolü mekanizması (UI/otomasyon YOK). Statü değişimi şimdilik admin elle.
- **Plan 13 Model 3 uygulandı:** firma-bağlamlı rol custom ClaimsPrincipalFactory ile.
- **Pre-commit gate disiplini:** dokunulan dosyadaki pre-existing antipattern (DateTime.Now, bare catch) commit'i bloklar → minimal hijyen düzeltmesi yapıldı (drive-by değil, gate zorunluluğu).

## Dikkat Edilmesi Gerekenler
- **SQL dosyası ≠ canlı VT** (memory: verify-against-live-db). Bu oturumda 3 kez işe yaradı: StockMovement Qty/ReferenceId yok, UserCompany.IsActive yok, UserCompany.Role='Admin'. Şema iddiasını her zaman `operax-cli query INFORMATION_SCHEMA` ile teyit et.
- **Kendini-açıklayan yorum** (memory: self-explanatory-comments) — kriptik marker + satır-no ref yasak; parent ADIYLA referans.
- Production C# servisleri (Receipt/Activity/DynamicBom) DEAD + kırık StockMovement INSERT'leri (Qty/ReferenceId kolonu yok). Gerçek üretim akışı SP (sp_ProductionFinish).
- Plan 14 guard HENÜZ canlı değil — post-SP enjeksiyonu yapılmadan dönem kilidi devrede değil (sadece çağrılırsa çalışıyor). Trigger emniyet ağı da kurulmadı.

## Yarına Başlangıç Noktası
1. **Plan 14 entegrasyon** — önce period trigger + SESSION_CONTEXT koordinasyonu tasarla, sonra post-SP guard enjeksiyonu (5 SP). Guard'ı canlı yapan adım.
2. AccountMovement IsDeleted kaldır (2 index rebuild) + StockMovement reversal SP.
3. Sonra MASTER_EXECUTION_PLAN sıradaki F0 modülü (M-F0.5 / sonraki) — master plan numarasını izle.
