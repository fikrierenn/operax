# Oturum Günlüğü — 2026-06-01

## Ana Konu
Cari/defter omurgası tamamlama (plan 18 açık-kalem kapama, plan 19 mutabakat) + FAZ 2 başlangıcı
(plan 21 irsaliye↔fatura N:1) + evrak denetimi (plan 22). Skill altyapısı genişletildi
(competitor-analyst). Skill-disiplini oturuma yerleşti: her Tier 3 öncesi reference-researcher +
mali-evrak-mevzuat + operax-erp-wms-auditor + competitor-analyst.

## Tamamlananlar
- **Plan 18 TAMAM** (açık-kalem kapama): AccountReconciliation tablosu (append-only, iki-yön index),
  sp_ReconcileMovements (kümülatif aşım guard) + sp_UnreconcileMovements (IsReversal ters satır),
  tvf_OpenItems + tvf_OpenItemAging, sp_AutoClosePayments auto-reconcile, 3 reversal SP recon guard,
  backfill, sql-sp-reviewer 3 fix. Ön-koşul: alış AM başlık-bazı (satış simetrisi) + v_ExpenseDistribution.
- **Plan 19 TAMAM** (cari mutabakat — M-F7.3 KAPANDI, FAZ 7'den öne çekildi):
  PartnerReconciliationLog (TTK md.94 SENT/CONFIRMED/DISPUTED/EXPIRED_CONFIRMED),
  sp_Create/RespondReconciliation, sp_GuardPartnerReconciled (CONFIRMED-only sert kilit, 8 SP enjeksiyon),
  tvf_ReconciliationPrep, Partner kartı mutabakat UI tab, sp_ExpireReconciliations + Hangfire günlük job.
- **AccountMovement Debit/Credit rename + IsDeleted kaldır** + DueDate/TaxAmount/NetAmount/CostCenterId/ExpenseTypeId.
- **Plan 21 Faz 1-3** (irsaliye↔fatura N:1 backend): ShippingLine.InvoicedQty + SalesInvoiceLine.SourceShipmentLineId
  + SalesInvoice.IssueDate; sp_GenerateSalesInvoiceFromShippings (N:1 + 7-gün VUK guard); tvf_UninvoicedShipments.
- **GERÇEK BUG fix:** sp_ShippingPost SourceDocType 'SHIPMENT'→'SHIPPING' (reversal/recon sessiz fail) + QtyOriginal pozitif.
  E2E zincir testiyle yakalandı (SO→2 irsaliye→N:1 fatura, 10 assert geçti).
- **Plan 22 Faz A** (P0): PO/SO tvf+vw POSTED tanıyor — Receiving PO dropdown 2→207 (kritik zincir bug).
- **competitor-analyst skill** oluşturuldu (rakip ERP parite denetçisi) + mali-evrak SKILL §3.4 bildirim kanalı mevzuatı.
- **Plan 20 + 22 yazıldı** (bildirim hub + evrak status/reversal).

## Build / Test Durumu
- Build: yeşil (her faz sonrası build-validator 0/0)
- Test: otomatik test yok (Operax.Web); E2E zincir SQL smoke yapıldı (irsaliye/fatura/SM/AM doğrulandı)
- Smoke: plan 18 (kapama+aşım+reversal), plan 19 (mutabakat+kilit), plan 21 (N:1 E2E) — hepsi geçti

## Commit Durumu
- Uncommitted: 0
- ~25 commit (plan 18/19/20/21/22 + bug fix + skill)
- Son: 5b7724a

## Aktif Planlar
- plans/18 — **TAMAM** (UI ekranı ayrı, backend bitti)
- plans/19 — **TAMAM** (Faz 1-6 + UI + Hangfire)
- plans/20 — plan hazır, implementasyon EN SON (kullanıcı kararı)
- plans/21 — Faz 1-3 backend ✅; Faz 4 (N:1 birleştirme UI) + Faz 5 (reviewer) KALDI
- plans/22 — Faz A ✅; Faz B düşürüldü (reversal inline guard, transition seed gereksiz);
  Faz C (7 reversal UI) + D (cari besleme fix) + E (reviewer) KALDI

## Yarım Kalan / Yarın'a Bırakılan İşler
- **Plan 22 Faz C (P0 — en yüksek muhasebe riski):** 7 reversal SP UI'sız (POSTED evrak iptal edilemiyor).
  C1: Shipping/Transfer/CycleCount/Production Details → OnPostCancelAsync + _DocToolbar "İptal Et" butonu.
  C2: SalesInvoice/Expense/Payment reversal UI. SP THROW 50000-59999 catch → TempData.
- **Plan 22 Faz D (cari besleme):** sp_PayLoanInstallment + sp_CloseStatement → AccountMovement yazmıyor;
  sp_PayCreditCardStatement CompanyId guard zayıf (IDOR).
- **Plan 21 Faz 4:** SalesInvoices birleştirme ekranı (sp_GenerateSalesInvoiceFromShippings UI) +
  irsaliyeli fatura (INSTANT TR-yaygın) + e-Belge DespatchDocumentReference (M16).
- **OPENING açılış fişi (M-F2.3):** competitor-analyst "tek gerçek go-live blocker" dedi — iade'den
  (M-F2.2/FIFO bağımlı) önce çekmeyi değerlendir. Master plan sırasıyla gerilim.

## Kararlar
- **Belge ayrımı prensibi (kullanıcı):** İrsaliye → StockMovement (cari borç YOK); Fatura → AccountMovement.
  Direkt mallı fatura (irsaliyesiz) StockMovement yazar (SourceShipmentLineId NULL + IsStockItem). Plan 21 §1.b.
- **PO+SO status: POSTED kanonik** — tvf/vw IN('POSTED','APPROVED') geçiş toleransı (migration riski sıfır).
- **Mutabakat sert kilit yalnızca CONFIRMED** (deep-research TTK md.94: sessiz onay yazılı cari hesap
  sözleşmesi olmadan hukuken zayıf). EXPIRED_CONFIRMED yumuşak (backlog).
- **FinancialTransaction soft-delete meşru** (sistem-içi kayıt), AccountMovement append-only (cari subledger).
- **Skill-first disiplini:** Tier 3 öncesi reference-researcher + mali-evrak + erp-wms-auditor + competitor-analyst.

## Dikkat Edilmesi Gerekenler
- **Denetim varsayımı ≠ kod gerçeği:** Plan 22 Faz B (transition seed) denetimden geldi ama reversal SP'leri
  ValidateStatusTransition çağırmıyor (inline guard) → faz düşürüldü. Her denetim bulgusu koda karşı doğrulanmalı.
- **Bash heredoc + Windows:** commit mesajı heredoc bazen bozuldu, .tmp unicode dosya oluştu (reset'lendi).
  `git commit -F-` <<'MSG' güvenli; `del`/`rm` permission denied → `git clean -f` kullan.
- **CLI JSON escape:** bash `\"` çift escape bozuyor; SP testleri için .sql script dosyası + operax-cli script kullan.
- **InvoiceMode=INSTANT:** sp_ShippingPost otomatik fatura kesiyor → N:1 birleştirme DEFERRED mod gerektirir.
- **SQL ≠ canlı VT:** SHIPMENT/SHIPPING tutarsızlığı + PO status APPROVED/POSTED canlıdan teyit edildi.

## Yarına Başlangıç Noktası
1. **Plan 22 Faz C1** — 4 WMS reversal UI (Shipping/Transfer/CycleCount/Production OnPostCancelAsync + buton).
   En yüksek muhasebe riski; SP'ler hazır, sadece UI bağı + _DocToolbar butonu.
2. **Plan 22 Faz C2** — SalesInvoice/Expense/Payment reversal UI.
3. **Plan 22 Faz D** — cari besleme fix (loan/card AM + card CompanyId IDOR).
4. Sonra Plan 21 Faz 4 (N:1 birleştirme UI) veya OPENING açılış fişi (go-live blocker).

---

# Oturum Günlüğü — 2026-06-01 (2. oturum)

## Ana Konu
Plan 22 Faz C1 (4 WMS reversal UI) + smoke testte ledger çift-sayım bug'ı yakalama/fix + STARTER kapsam netleştirme → Plan 23 (Şube/Branch) yazımı.

## Tamamlananlar
- **Plan 22 Faz C1:** Shipping/Transfer/CycleCount/Production Details'e `OnPostReverseAsync` + "İptal Et" butonu (confirm). Transfer/CycleCount/Production'a ILogger, Production'a IAuditService DI eklendi.
- **migrate fix:** `db_objects_reversal.sql` CLI migrate listesinde yoktu → eklendi (`src/Operax.Cli/Program.cs`). 7 reversal SP canlı DB'ye hiç uygulanmamıştı.
- **Ledger çift-sayım fix (kritik):** 5 StockMovement reversal SP'si hem IsCancelled=1 flag hem -QtyBase REVERSAL satırı yazıyordu; tvf_InventoryBalance IsCancelled=0 filtreler → stok reversal sonrası 2× geri geliyordu (smoke ile +100 doğrulandı). Fix: ters-satır kaldırıldı, flag-only model (guard UPDATE rowcount'a taşındı). Re-smoke: aktif net=0 ✓.
- **Altyapı:** `_Layout` global TempData banner + yeni `_alerts.css` part (sessiz hata fix); `document-immutability.md` §1.b StockMovement flag-only vs AccountMovement ters-satır netleşti.
- **Commit:** `f56361f` (16 dosya).
- **Plan 23 yazıldı:** `plans/23-branch-multilocation-transfer.md` — Şube(Branch) tablosu + Warehouse.BranchId + 4 transfer tipi (competitor-analyst onaylı Option 1).

## Build / Test Durumu
- Build: **0 hata 0 uyarı** (Web + Cli)
- Smoke: Shipping reversal uçtan uca (gerçek POSTED kayıt, SP → Status=CANCELLED + flag-only doğrulandı, demo veri geri yüklendi)
- CycleCount/Production: COMPLETED kayıt yok → buton görünmez, smoke yapılamadı (mantık Shipping ile aynı)

## Aktif Planlar
- plans/22 — Faz A done, B düşürüldü, **C1 done (f56361f)**; C2/D/E kaldı
- plans/23 — **YENİ, taslak, onay bekliyor** (Şube/Branch)

## Yarım Kalan / Yarına Bırakılan
- Plan 23 onay + implement (Faz A şema → E smoke). Şube STARTER'a dahil edildi.
- Plan 23 açık karar: belge-seviye şube boyutu (fatura/cari BranchId) ayrı plan 24 mı, Plan 23 Faz F mi?

## Kararlar
- **Şube modeli = Option 1** (ayrı Branch tablosu + Warehouse.BranchId; stok Depo'da kalır, ledger ripple yok). competitor-analyst: Mikro/Logo şubeyi dik org boyutu tutar (subeno+depono), şube N depoya sahip. "Şube=depo tipi" reddedildi.
- **Transfer STARTER kapsamında** (temel stok M02, ileri WMS olmasa bile). Depo+şube+4 transfer tipi STARTER.
- **Ertelendi (kapsam dışı):** belge-seviye şube boyutu, şubeler arası intercompany satış, ileri WMS (M05-M09).
- **Reversal düzeltme modeli:** StockMovement = flag-only (IsCancelled, ters-satır YOK); AccountMovement = ters-satır (IsCancelled kolonu yok). İkisi append-only.

## Dikkat Edilmesi Gerekenler
- **Çift-sayım dersi:** Reversal SP'si hem flag hem ters-satır yazarsa, balance IsCancelled=0 filtreliyorsa stok 2× geri gelir. Yeni stok-iptal SP'lerinde tek mekanizma (flag) kullan.
- **migrate dosya listesi eksikti:** Yeni `db_objects_*.sql` yazınca CLI Program.cs migrate sırasına eklemeyi unutma — yoksa SP canlıya gitmez.
- **Smoke şart:** Bu oturum build 0/0 olmasına rağmen smoke iki ayrı kritik bug ortaya çıkardı. SP işinde smoke atlanmaz.
- **Kapsam drift:** Plan 22 Faz C1 4 WMS'i tek grup almıştı; sadece Shipping STARTER. Plan grupları modül-paket sınırını gözetmeli.

## Yarına Başlangıç Noktası
1. **Plan 23 onayı** — Şube/Branch modeli (Option 1) + belge-seviye şube kararı (Faz F mi, plan 24 mü). Onay sonrası Faz A şema.
2. Alternatif: **Plan 22 Faz C2** (SalesInvoice/Expense/Payment reversal UI — hepsi STARTER) — Faz C1 ile aynı pattern, hızlı.
3. STARTER odak korunacak (M00/01/02/03/04/11).

---

# Oturum Günlüğü — 2026-06-01 (3. oturum — uzun)

## Ana Konu
STARTER zincir tamamlama: Plan 22 reversal kuyruğu → Plan 23 Şube → Plan 05 belge zinciri → Plan 24 Mal Alım Faturası → Plan 26 Gider Raporu → Plan 25 Sarf Fişi. Her plan öncesi skill (competitor-analyst + mali-evrak + reference-researcher) + her faz sonrası review zinciri (code/sql/security).

## Tamamlananlar
- **Plan 22 (C2/D/E):** SalesInvoice+ExpenseInvoice reversal UI; sp_CloseStatement+sp_PayCreditCardStatement IDOR fix (@CompanyId param); smoke (SP param HeaderId→InvoiceId bug yakalandı).
- **Plan 23 (Şube A-F):** Branch tablosu + Warehouse.BranchId + 4 transfer tip türetme (TransferTypeHelper) + belge/ledger BranchId. Trigger kaldırıldı → SP'lerde açık subquery (ISNULL(Warehouse.BranchId, fn_DefaultBranchId)). UIX_Branch_Merkez_PerCompany.
- **Plan 05 (belge zinciri 4 faz):** _DocFlowButtons partial + sp_CreateReceivingFromPO/ShippingFromSO; smart button sayaç + ön-dolu DRAFT + ödeme zinciri.
- **Plan 24 (PurchaseInvoice) ARŞİV:** mal alım faturası ayrı tablo (ExpenseInvoice gider modeli mal taşıyamıyordu — boş fatura bug'ı çözüldü). SupplierInvoiceNo/Date/Uuid, satır-bazlı N:1 Receiving bağı, AccountMovement borç TEK NOKTA. ItemType CONSUMABLE.
- **Plan 26 (Gider Raporu) ARŞİV:** ExpenseType ağaç + ExpenseInvoice.CostCenterId (başlık) + Line.CostCenterId NULL (COALESCE miras) + tvf_ExpenseBreakdown (GL'siz, belge katmanından) + Report UI.
- **Plan 25 (Sarf Fişi/MaterialIssue) ARŞİV:** StockMovement ISSUE only (AccountMovement YOK — iç tüketim); negatif stok guard (THROW 51554); bin fallback stoklu bin; flag-only reversal; CRUD+RBAC+sidebar. BKM senaryosu: STOCK+CONSUMABLE sarf edilir, sarf evrağına giren=sarf.
- **Bonus fix:** PurchaseInvoices+MaterialIssue ModuleKeys'e eklendi (Plan 24 RBAC kaçığı); Plan 05 ExpenseInvoice.IsDeleted migrate fail (sessiz) yakalandı/düzeltildi.
- **Yeni kural:** .claude/rules/phase-review-gate.md — faz kapanış zorunlu kontrol zinciri.

## Build / Test Durumu
- Build: **0 hata** (Web)
- Smoke: Plan 22 (SI/EI reversal), 23 (4 transfer tip), 24 (Receiving→PurchaseInvoice satır+AM borç), 25 (sarf ISSUE + 999999 red + flag reversal), 26 (CostCenter×tip kırılım) — hepsi geçti, demo veri temizlendi.

## Aktif Planlar (arşivlenmemiş ama bu oturumda commit'li)
- plans/05 — Faz 1-4 done; Receiving→Invoice kısmı Plan 24'e devredildi (arşiv değerlendirilmeli)
- plans/22 — C2/D/E done (C1 önceki oturum); arşiv değerlendirilmeli
- plans/23 — A-F done; arşiv değerlendirilmeli
- archive: 24, 25, 26 (TAMAM)

## Yarım Kalan / Yarına Bırakılan
- **SO/Shipping CONSUMABLE satış-gizleme filtresi** (Plan 25 not — ürün satışta gizleme, ileride).
- **Tailwind utility salatası sistemik borç** (TODO'da) — fatura/rapor view'ları semantic class refactor (text-indigo-600 token değil).
- Plan 05/22/23 arşive taşınması (done ama plans/ altında).
- N:1 Receiving→Invoice birleştirme UI (şema destekler, seçim ekranı yok).

## Kararlar
- **Mal alım ≠ gider faturası:** PurchaseInvoice (stoklu mal/sarf) ayrı; ExpenseInvoice (hizmet/stoksuz) ayrı. ItemType=STOCK/CONSUMABLE/SERVICE/FIXED_ASSET.
- **ItemType davranış filtresi, kısıt değil (BKM):** STOCK hem satılır hem sarf; CONSUMABLE sadece sarf (satışta gizli); SERVICE stoksuz. Sarf fişi STOCK+CONSUMABLE alır.
- **Cari besleme tek nokta:** AccountMovement borç YALNIZCA fatura POST'unda; irsaliye/mal kabul/sarf cari beslemez (R0/K3 drift kapandı).
- **Sarf tüketimi GL'siz:** StockMovement ISSUE + maliyet; gider hesabı mahsubu GL açılınca (SourceDocType=CONSUMPTION köprü). AccountMovement YAZILMAZ.
- **AccountMovement.IsDeleted kaldırma kasıtlı:** cari defter TTK md.65 silme yasağı → REVERSAL ters-satır.

## Dikkat Edilmesi Gerekenler
- **SP param adı ≠ C# param:** Plan 22'de HeaderId/InvoiceId uyuşmazlığı smoke'ta yakalandı. SP imzasını C# çağrısıyla doğrula.
- **Yeni db_objects_*.sql → CLI migrate listesi:** Plan 24/25/26 hepsinde Program.cs'e eklendi. Unutma.
- **Migrate batch sessiz fail:** ExpenseInvoice.IsDeleted yok → filtered index fail; CREATE OR ALTER son-kazanır (v_ExpenseDistribution 3 dosyada).
- **negatif stok guard:** sarf fişinde var (51554); ileride serbest/manuel mod parametresiyle gevşetilebilir.

## Yarına Başlangıç Noktası
1. **Plan 05/22/23 arşive taşı** (done, plans/ temizliği) — git mv archive/.
2. SO/Shipping CONSUMABLE satış-gizleme filtresi (küçük, Plan 25 kuyruğu).
3. Sonraki STARTER eksiği: cari ekstre/yaşlandırma (M11 ⚠️) veya N:1 fatura birleştirme UI.

---

# Oturum Günlüğü — 2026-06-01 (4. oturum)

## Ana Konu
"Sırayla devam + STARTER'dan hiçbir şey kalmasın" → STARTER E2E tam doğrulama + N:1 fatura UI + güvenlik runtime smoke. Sonra deploy/hosting danışmanlığı (Hostinger/VPS/ücretsiz) + PostgreSQL geçiş fizibilitesi + pgsql-porter agent.

## Tamamlananlar
- **Plan 05/22/23/17/21 arşive** — hepsi canlı kodla "done" doğrulandı (stale checkbox pattern), TAMAM marker + git mv.
- **CONSUMABLE satış-gizleme** (plan-25 kuyruğu): SO+Shipping item dropdown + add-line guard `ItemType<>'CONSUMABLE'`; runtime tarayıcı testi geçti (dropdown'da sarf yok + crafted POST guard mesajı).
- **Plan 21 Faz D — N:1 fatura birleştirme UI**: `SalesInvoices/Create` (tvf_UninvoicedShipments + sp_GenerateSalesInvoiceFromShippings) + `_merge.css`; `schema_M04_ShipmentInvoiceMerge.sql` CLI migrate listesine eklendi (temiz kurulum kırıktı). Tarayıcı E2E: 3 sevkiyat→kısmi seç→INV-2026-00001 + ledger Debit 11430.
- **Seed fix**: ShippingHeader PartnerId+SalesOrderId eksikti (N:1 demoable).
- **F0.2 güvenlik** canlı koddan doğrulandı (RL/SH/RBAC/AP/TLS hepsi VAR — SH-1 NetEscapades). **3 GERÇEK auth bug** runtime smoke ile bulundu+düzeltildi: LoginPath `/Auth/Login`→`/login` (404), AccessDenied sayfası yoktu (eklendi), Kestrel Server header (AddServerHeader=false).
- **STARTER E2E tam doğrulama** (transaction+rollback smoke): Faz1 Loan 3 method (12 taksit), Faz2 Purchase→Receiving→ItemCost.AvgCost, Faz4 Shipping→Invoice, Faz7 Payment loop (tx+AM Credit+FIFO kapama). Plan 01 TAMAM+arşiv.
- **review-pr** (3 paralel agent): 2 CRITICAL silent-failure benim guard'larımda → düzeltildi (LogWarning+TempData). Review-debt #5 (60000+ ex.Message) FALSE çıktı (reddedildi). Pre-existing borçlar TODO'ya.
- **operax-erp-wms-auditor** tam tarama (4 paralel agent): STARTER Olmazsa-olmaz %100. Agent'ların "eksik SP" CRITICAL'leri (sp_DepositNote/CycleCountPost/ShippingCreatePickTask) **FALSE** — SQL+canlı DB'de VAR (agent sadece .cs grep'ledi). Gerçek: 3 dead production servisi + 5 boş placeholder klasör (sidebar linki yok).
- **ForwardedHeaders middleware** eklendi (reverse proxy/Hostinger HTTPS+cookie deploy şartı).
- **pgsql-porter agent** + `docs/postgres-pilot.md` + agent-usage matris satırı.

## Build / Test Durumu
- Build: yeşil (0/0, ForwardedHeaders sonrası son doğrulama). Sonraki değişiklikler docs/agent — build etkilemez.
- E2E smoke: STARTER zinciri (purchase/loan/payment) + N:1 merge + CONSUMABLE + rate-limit + auth → hepsi geçti.
- Tarayıcı testi (preview): login + merge + AccessDenied + CONSUMABLE dropdown — geçti.

## Commit Durumu
- Uncommitted: 0
- Bu oturum ~12 commit (CONSUMABLE, N:1 UI, seed, auth fix, plan arşivleri, pgsql tooling).

## Aktif Planlar
- plans/ temizlendi — 01/05/17/21/22/23 arşive taşındı (hepsi TAMAM).
- Açık plan: postgres-migration (henüz yazılmadı — kullanıcı karar verecek dual vs tek-hedef).

## Yarım Kalan / Yarın'a Bırakılan İşler
- **PostgreSQL geçiş kararı bekliyor**: tek-hedef PG (~2-4 hafta) vs dual MSSQL+PG selectable (~4-6 hafta + 2× bakım). Kullanıcı "müşteri motor seçsin mi" netleştirecek. pgsql-porter agent hazır (sonraki oturumda aktif).
- **Dead production servisleri** (ProductionReceipt/Activity/DynamicBom) silme kararı — WIP atölye planı varsa SP'ye taşı, yoksa sil.
- TODO review-borçları (5): OnPostAsync hata yönetimi, audit izolasyon, Index:96 inline, PartnerType sabiti, CancellationToken.
- **Dev datası kalıntı**: INV-2026-00001 + 3 sevkiyata partner (merge testinden, kalıcı). Temizlik gerekebilir.

## Kararlar
- **STARTER fonksiyonel olarak eksiksiz** — 7 faz E2E kanıtlı, plan 01 arşiv.
- **DB değiştirme = EF değil**: SQL-first mimaride logic 53 SP'de → EF rewrite (aylar). PG'ye dialect-çeviri (haftalar) mimari korur. Pilot kanıtladı.
- **En ucuz hosting**: MSSQL Express ücretsiz; gerçek bedava 7/24 = self-host veya (PG'ye geçilirse) Oracle Cloud Free ARM 24GB.
- **pgsql-porter agent**: dual-engine veya tek-hedef her ikisinde de çeviriyi asiste eder.

## Dikkat Edilmesi Gerekenler
- **Stale checkbox pattern 5× tekrarladı** bu oturum (plan 05/22/23/17/01 + F0.2 + M11 ekstre): plan "Taslak/Onay Bekliyor" ama iş bitmiş. Karar almadan ÖNCE canlı koddan doğrula (todo-verification kazandı).
- **Agent grep tuzağı**: denetim agent'ları sadece .cs grep'leyip "SP yok" dedi — SQL dosyalarına bakmadılar. Dış analiz = hipotez; doğrula.
- **Yeni agent mid-session aktif olmaz** — registry oturum başında yüklenir. pgsql-porter sonraki oturumda invoke edilebilir.
- **Migrate listesi tuzağı yine**: schema_M04_ShipmentInvoiceMerge.sql listede yoktu → temiz kurulum kırıktı (eklendi).

## Yarına Başlangıç Noktası
1. **PostgreSQL kararı**: kullanıcıya sor — tek-hedef mi dual-engine mi? Karara göre `plans/NN-postgres-migration.md` yaz (pgsql-porter pipeline + 6 faz).
2. **Veya** dead production servisleri kararı (sil/SP'ye taşı) + TODO review-borçları temizliği.
3. **Veya** "arkadaşa test ettir" → cloudflared tunnel ile canlı link (app + ForwardedHeaders hazır).
