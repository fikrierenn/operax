# Oturum Günlüğü — 2026-06-02

## Ana Konu
Çok uzun, yoğun oturum. Neon PG pilotu → ekran UX iyileştirme (otomatik doldurma) →
gerçek tarayıcı E2E ile bug avı → fatura fiyat farkı + **yerel AI (Qwen3-4B) gerekçe
denetimi** → global ondalık para bug'ı → POSTED fatura gerekçeli düzeltme.

## Tamamlananlar (commit'li)
- **Neon PostgreSQL pilot** (`pg-test` CLI) — `d27fe60`. PG geçişi **ERTELENDİ**, MSSQL'de kalınıyor ([[postgres-migration-parked]]).
- **3 bug fix** (`10c772a`): tedarikçi listesi boş (Partner.Type SUPPLIER→VENDOR), OrderDate `DateTime.MinValue` SqlDateTime overflow, City mojibake (N'...' prefix + DB temizlik).
- **Ekran UX** (`759cb3a`): PO satır fiyat otomatik (ItemCost.AvgCost), Mal Kabul birim otomatik (BaseUomId) + sipariş seçince depo/tedarikçi otomatik + açık sipariş tedarikçi-filtre/arama; `sp_ReceivingPost` bin fallback + her depoya KABUL bin'i seed; "EACH"→Türkçe birim; DRAFT mal kabul Sil. `screen-ux-standard` skill.
- **Receiving iç içe form bug** (`c9f3c83`): `_DocFlowButtons` header `<form>` içindeydi → "Alış Faturası Oluştur" header'ı submit ediyordu. Form dışına taşındı.
- **Fatura kalem fiyat düzenleme (DRAFT)** + fiyatsız onay guard THROW 51536 (`4dfc97b`).
- **tr-TR ondalık 10x para bug**: önce fatura tekil (`322bb51`), sonra **GLOBAL `InvariantDecimalModelBinder`** (`6f0fe3e`) — number input '12.5'i tr-TR 125 okuyordu, tüm ekranlarda çözüldü.
- **YEREL AI ÇALIŞIYOR** (`6f0fe3e`): Qwen3-4B-Q4_K_M, LLamaSharp 0.27 süreç-içi. Fiyat farkı gerekçe denetimi canlı doğrulandı — anlamsız→IMPLAUSIBLE, makul→PLAUSIBLE + Türkçe yorum. Kök neden 0-token: **HttpContext.RequestAborted** yavaş inference'i kesiyordu → bağımsız 120s CTS. Doğru pattern: StatelessExecutor + `ApplyTemplate=true` + SystemMessage + DefaultSamplingPipeline + AntiPrompts=["<|im_end|>"] + `/no_think` + `<think>` regex temizleme. `local-llm-integration` skill.
- **Plan 27 Faz A-D** (`1912f7f`): PriceVariance şema (OverrideReason/AiVerdict/AiComment), `sp_PurchaseInvoicePost` PO↔fatura fiyat kıyas (**TOLERANS YOK**), AI client, UI fark kartı + gerekçeli override.
- **POSTED fatura gerekçeli düzeltme** (`9d7f2b4`, sonra revize — aşağıya bak).

## Build / Test Durumu
- Build: ⚠️ **SON DEĞİŞİKLİK BUILD EDİLMEDİ** (preview durduruldu, oturum kapandı). Sabah ilk iş `dotnet build`.
- Smoke: gün içi çok sayıda gerçek tarayıcı (preview) E2E — PO→mal kabul→fatura→düzeltme, AI verdict, ondalık parse, fatura fiyat farkı. Hepsi geçti (build edilmemiş SON revizyon hariç).

## Commit Durumu
- Uncommitted: 3 (düzeltme revizyonu — SP in-place + handler/view metin; build edilmedi).
- Bu oturum ~9 commit (d27fe60 → 9d7f2b4).

## SABAH İLK İŞ — DOĞRULA
- **`sp_CorrectPurchaseInvoiceLine` SON HALİ**: ters/delta kayıt **KALDIRILDI** (kullanıcı kararı: alış faturası veri-giriş hatası + ödeme yok + GİB'e gitmemiş → ters kayıt gereksiz, cari ekstre temiz kalsın). Artık `AccountMovement` satırı **YERİNDE UPDATE** (Credit/NetAmount/TaxAmount yeni tutara), iz **audit log**'da. SP DB'ye uygulandı + test verisi temizlendi (ALN-20260601-210240 tek satır 11.700).
- C# handler + view metinleri "ters+yeni" → "yerinde güncelleme" değişti **ama BUILD EDİLMEDİ**. Sabah: `dotnet build` + taze smoke (kalem düzelt → AM tek satır yeni tutar, audit eski→yeni).

## Aktif Planlar
- plans/27 — Faz A-D done; Faz E (review zinciri + tam smoke) + aşağıdaki açık işler kaldı. Arşive HENÜZ taşınmadı.

## Yarım Kalan / Yarın'a Bırakılan İşler
1. **PO bağı YOKSA da PriceList (satınalma şartı) fiyatından sapma → variance** — şu an sadece PO-bağlı satırda fark üretiliyor. `sp_CheckPriceVariance` PriceList ile kıyaslıyor ama fatura post'unda çağrılmıyor; wire et.
2. **Vade tarihi tedarikçi kartından otomatik** — fatura DueDate `Partner.PaymentTermDays` anlaşması üzerinden gelmeli.
3. **Düzeltme sonrası variance YENİDEN HESAPLANMALI** — `sp_CorrectPurchaseInvoiceLine` fiyatı değiştirince mevcut PriceVariance güncellenmiyor; düzeltilen fiyat ↔ PO fiyat tekrar kıyaslanıp revize edilmeli.
4. **95 ekrana YARDIM BUTONU + KULLANICI KİTAPÇIĞI** — büyük, ajanlarla toplu.

## Kitapçık / Yardım Butonu Bulguları (envanter hazır)
- **95 Razor sayfası** envanterlendi (14 modül): M00 Admin 16, Auth 3, Dashboard 1, MasterData 11, M02 Stok 8, M03 Satınalma 8, M04 Satış 8, M08 Sayım/Transfer 8, M10 Picking 3, M11 Finans 16, M12 Üretim 7, M13 Gider 3, M14 Bütçe 2, M16 Sarf 2.
- **Yardım butonu yeri**: `Features/Shared/_PageHeader.cshtml` (satır 48-51 `ActionsHtml`, sağ üst). `PageHeaderVm`'e opsiyonel `ScreenId` ekle → merkezi `/Help/{screenId}` endpoint her ekrana özel Markdown yardım sunar.
- **Öneri**: `docs/manual/` altında modül-modül Markdown kitapçık + ekran-içi yardım **aynı Markdown'dan** beslensin (tek kaynak).

## Kararlar
- **PG ertelendi, MSSQL kalıcı** — pilot "yapılabilir" dedi, yeterli.
- **Fiyat farkında tolerans YOK** — anlaşılan PO fiyatı bağlayıcı; her sapma variance.
- **AI = yalnızca yerel qwen3** (bulut yok), soft-fail (down → UNCHECKED, iş bloke olmaz).
- **Veri-giriş düzeltmesinde ters kayıt YOK** (kullanıcı): alış faturası + ödeme yok → AM satırı yerinde düzeltilir, iz audit'te. (mali-evrak-mevzuat ilk önerisi ters-kayıttı; kullanıcı pragmatik kararı: gereksiz.)

## Dikkat Edilmesi Gerekenler
- **AI build edilmedi** — sabah ilk `dotnet build`.
- `appsettings Ai:Enabled=TRUE`. Model `App_Data/models/llm/Qwen3-4B-Q4_K_M.gguf` (gitignore'lu, 2.5GB). Temiz makinede model yoksa soft-fail UNCHECKED. İlk inference ~60-90s (CPU + model yükleme).
- Eski `qwen25-3b-instruct-q4_k_m.gguf` klasörde — silinebilir.
- **Neon connection string chat'te açıldı, şifre RESET edildi** (yeni şifre User Secrets `Neon:ConnectionString`).
- Dev test datası kaldı: ALN-20260601-210240/210729, RCV-2026-001 faturası, birkaç PriceVariance.
- Yeni skill: `screen-ux-standard`, `local-llm-integration`.

## Yarına Başlangıç Noktası
1. **`dotnet build`** — son düzeltme revizyonunu doğrula + taze smoke (kalem düzelt → AM tek satır).
2. Düzeltme revizyonunu commit et (build yeşilse).
3. Açık iş 1-3 (PriceList variance + vade tedarikçiden + düzeltme sonrası variance recompute) — küçük SP işleri, birlikte ele alınabilir.
4. Sonra: 95 ekran yardım butonu + kitapçık (ajan fan-out).

## Compact Snapshot --- 11:23

### Son 5 Commit

- db23722 feat(plan-28 FazB): sp_ReceivingPost ReturnQty → iade/karantina bin'i
- 2abd733 feat(plan-28 FazB): sipariş kontrollü terminal tarama (3 mod) + iade alanı
- 3275e73 feat(plan-28 FazA): mal kabul şema — ReceivingMode + tedarikçi irsaliye no/tarih + ReturnQty (iade alanı)
- 8c16ac6 docs(plan-28): skill denetimi (mali-evrak + competitor) — irsaliye no/tarih + fazla-mal akış fix + pazar parite verdict
- af1eedb docs(plan-28): 3 mod (tek PO / toplu kör kabul / serbest) + fatura-aşaması PO eşleştirme

### Uncommitted Dosya Sayısı

- 4 uncommitted dosya

### Aktif Planlar

- 02-m11-finance-create-forms.md
- 06-expense-service-analytic-accounting.md
- 07-ai-assistant-rag-alarms.md
- 08-partner-card-tabs.md
- 09-cari-hesap-defteri-accountmovement.md
- 12-data-isolation-guard.md
- 13-switch-company-hardening.md
- 14-ledger-pk-immutability.md
- 15-data-dictionary.md
- 16-account-movement-auto-feed.md
- 18-open-item-reconciliation.md
- 19-partner-reconciliation-statement.md
- 20-notification-hub.md
- 27-invoice-po-price-variance-ai.md
- 28-receiving-terminal-po-control.md

### Aktif TODO (ilk 15 açık madde)

- - [ ] **Düzeltme revizyonu BUILD + smoke** — `sp_CorrectPurchaseInvoiceLine` ters kayıt kaldırıldı, AM yerinde UPDATE. C# build edilmedi (oturum kapandı).
- - [ ] **PO bağı YOKSA PriceList sapması → variance** — `sp_CheckPriceVariance` PriceList kıyaslıyor ama `sp_PurchaseInvoicePost`'ta çağrılmıyor; wire et.
- - [ ] **Vade tarihi tedarikçi kartından** — fatura DueDate `Partner.PaymentTermDays` üzerinden otomatik.
- - [ ] **Düzeltme sonrası variance yeniden hesapla** — `sp_CorrectPurchaseInvoiceLine` fiyat değişince PriceVariance revize etmiyor.
- - [ ] **HIGH · hata yönetimi yok:** `SalesOrders/Details.cshtml.cs` `OnPostAsync` (kaydet, NumberSeries+INSERT korumasız) + `OnPostAddLineAsync` (INSERT+audit try/catch yok). Approve/Cancel pattern'ini uygula.
- - [ ] **HIGH · audit izolasyonu DOĞRULANMADI:** `IAuditService.LogAsync` caller'ı patlatıyor mu? İzole değilse audit hatası satır eklemeyi geçersiz kılar. Kaynak okunmalı.
- - [ ] **HIGH · inline style:** `SalesInvoices/Index.cshtml:96` `style="color:@dueColor;"` Razor expression — class'a çevir (`text-danger`/`text-2`). "Touch ettikçe azalt".
- - [ ] **MED · magic string:** `SalesOrders/Details.cshtml.cs:42` `'CUSTOMER'`/`'BOTH'` — `Dtos.cs`'e `PartnerType` sabiti ekle.
- - [ ] **MED · CancellationToken:** handler'lar `ct` almıyor, `OperationCanceledException` rethrow yok (`error-handling.md §4-5`). Disiplin borcu.
- - [ ] **DEAD/WIP production servisleri** (ProductionReceiptService, ProductionActivityService, DynamicBomService) — caller/DI yok, kırık StockMovement INSERT'leri (canlı VT'de `Qty`/`ReferenceId` kolonu yok, NOT NULL CompanyId/UomId/QtyOriginal eksik). Şimdilik guard'da `isolation-guard:ignore` (dead/WIP gerekçesi) ile bastırıldı. **Karar ertelendi:** WIP atölye terminali feature'ı planlanırsa SP'ye taşınarak (`sp_ProductionConsume`/`sp_ProductionInspect`) yeniden yazılmalı; planlanmazsa silinmeli. Architecture §4: ham C# stok insert yasak, SP zorunlu.
- - [ ] **Sprint-kapanış (plan 12 §4):** dead servisler çözülünce guard 0 → blocking hook'a bağla (pre-commit/session-start).
- - [ ] **Periyodik GL muhasebeleştirme modülü** — subledger→GL aylık/seçimli posting (K1). **Ön koşul: muhasebe-mevzuat skill'i** (VUK / e-Defter tebliğleri / hesap planı standardı / berat / GİB formatları). e-Defter ÜRETİMİ kapsam dışı (K5 — Operax sadece LOCKED döneme saygı gösterir). **Posting-rule deseni netleşti** (Mikro §3.5, `docs/reference/MIKRO_V16_ANALYSIS.md`): 3 yapı taşı = HesapPlani + PostingRule(grup+hareket tipi→hesap kodu, normalize) + masraf merkezi boyutu; muhasebeleştirme SP subledger hareketini grup+yön→hesap eşleyip işaretli meblağla fişe yazar, `fis_ticari_uid` ile geri-bağlar.
- - [ ] **En yüksek 4 (üretilmeli):** E1 irsaliye↔fatura ayrımı+dönüşüm (VUK) · E2 alış/satış iade (ayrı belge+ters-kayıt) · E4 fire/zayi/imha (maliyet+vergi) · E11 virman kasa↔kasa/cari↔cari (Plan 11 başlamadı).
- - [ ] **Orta:** E5 sayım fazla/eksik · E7 stok açılış/devir · E12 vade farkı/borç-alacak dekontu.
- - [ ] **Çek statü gap:** TEMİNAT (sck_sonpoz=3) + KISMİ ÖDEME (=9) → Cheque statü makinesine + document-immutability §2.4.

