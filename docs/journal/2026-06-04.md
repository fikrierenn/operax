# Oturum Günlüğü — 2026-06-04

## Ana Konu
İki paralel denetim workflow'u (SP iş-doğruluğu + C# mimari bütünlük) → bulguları adversarial doğrula → Plan 33 yaz → Faz A + Faz B düzeltmeleri uygula.

## Tamamlananlar
- **Denetim workflow #1 (SP):** 33 ajan, 55 SP, 41 bulgu → 9 onaylı HIGH/CRITICAL (adversarial 2-refuter). CRITICAL: transaction disiplini (8 SP TRY/CATCH'siz), çift-post (idempotency yok), ledger immutability (sp_CorrectPurchaseInvoiceLine yerinde UPDATE).
- **Denetim workflow #2 (mimari):** 35 ajan, 99 PageModel+Lib, 77 bulgu → 12 onaylı. CRITICAL: Putaway/Picking terminal C#'tan StockMovement INSERT (SQL-First bypass).
- **Plan 33** yazıldı: `plans/33-sql-arch-conformance-fixes.md` — 6 faz, 21 kanıtlı bulgu, 5-lens.
- **Faz A (commit b22db21)** — ledger immutability:
  - C3+H1: `sp_CorrectPurchaseInvoiceLine` AccountMovement yerinde UPDATE → ters REVERSAL + yeni doğru satır (append-only, CompanyId+Currency). Mükerrer-düzeltme bakiye drift'siz.
  - H2: `sp_PurchaseInvoiceReverse` Currency hardcoded 'TRY' → faturadan.
  - H5: `sp_MaterialIssueReverse` CancelledBy TRY_CAST (canlı VT: uniqueidentifier).
- **Faz B (commit ce9f318)** — WMS terminal SQL-First:
  - AC1 `sp_PutawayPost` (yeni): Putaway.cshtml.cs C# ledger INSERT kaldırıldı → SP (dönem kilidi + negatif-stok guard THROW 51570).
  - AC2 `sp_PickConfirm` (yeni): Picking/Terminal.cshtml.cs barkod+durum SP'ye, davranış birebir (StockMovement yazmaz).
  - AH4: Transfer/Terminal.cshtml.cs boş catch{} → catch(SqlException)+ILogger.
  - CRIT-1: StockTransfer.CreatedBy TRY_CAST · IMP-1: BaseUomId guard.
  - Yeni dosya `db_objects_putaway_pick.sql` + migrate listesi #13 (Program.cs).

## Build / Test Durumu
- Build: **yeşil** (0 hata, 1 pre-existing uyarı — Program.cs ForwardedHeaders, ilgisiz).
- Test: otomatik test yok (proje durumu).
- Smoke: **Putaway yapıldı** — 510→508 (-2) / ToBin +2, net-korundu; negatif-stok guard THROW 51570 doğrulandı. Pick smoke: açık PickTask seed yok → yapılamadı (DEBT).

## Commit Durumu
- Uncommitted: 0 (handoff journal commit'i hariç).
- Bugünkü commit'ler: b22db21 (Faz A), c00fb3c (Faz A TODO), ce9f318 (Faz B), 279593e (Faz B TODO).

## Aktif Planlar
- `plans/33-sql-arch-conformance-fixes.md` — Faz A ✅ · Faz B ✅ · Faz C/D/E/F kaldı.

## Yarım Kalan / Yarın'a Bırakılan İşler
- **Faz C** — 8 SP TRY/CATCH+ROLLBACK+THROW normalizasyon (C1) + status guard + StockMovement idempotent UNIQUE (C2). En sistemik iş.
- **Faz D** — `Lib/DocumentLock.cs` helper (henüz YOK) + PO/SO/Shipping/CycleCount edit guard (AH6/AH7).
- **Faz E** — THROW 60xxx→50xxx (H3) + DEAD servis sil (ProductionReceiptService/ActivityService, DI+caller yok doğrulandı).
- **Faz F** — StockMovement.MovementDate kolonu + trigger simetri (H6s, şema + backfill). En büyük.

## Kararlar
- **Pick stok-düşme semantiği (DEBT):** sql-sp-reviewer flagledi — `sp_PickLinePost` StockMovement ISSUE yazıyor, `sp_PickConfirm` yazmıyor. Doğru semantik: stok çıkışı yalnız sevkiyat POSTED (`sp_ShippingPost`). `sp_PickLinePost` ISSUE'su çift-sayım riski → ayrı denetim. TODO'ya işlendi.
- **Putaway idempotency:** ad-hoc fiziksel aksiyon, kaynak belge yok → UNIQUE koruması uygulanamaz, DEBT değil (domain doğru, sql-sp-reviewer onayı).
- **sp_PickConfirm StockMovement yazmaması:** bilinçli — pick fiziksel hazırlık, stok sevkte düşer (reviewer onayı).

## Dikkat Edilmesi Gerekenler
- **Test şirketi 00000...** stok verisi içeriyor (510 adet); gerçek şirket d1e1b1a5. Smoke 0000 üzerinde yapıldı.
- **CreatedBy/UpdatedBy tip asimetrisi:** StockTransfer.CreatedBy uniqueidentifier, UpdatedBy nvarchar. Identity user.Id NVARCHAR(450). Yeni SP'lerde TRY_CAST kullanıldı; eski kod implicit cast'le çalışıyordu (GUID-string).
- **sp_CorrectPurchaseInvoiceLine kırılganlık (reviewer notu):** @OldGrand SELECT'i başlık UPDATE'inden ÖNCE olmalı; biri sırayı bozarsa bakiye drift eder.

## Yarına Başlangıç Noktası
1. **Faz C başla** — 8 SP'yi `sp_ReceivingPost` desenine (TRY/CATCH+ROLLBACK+THROW) getir: sp_ShippingPost, sp_ShippingCreatePickTask, sp_TransferPost, sp_CycleCountPost, sp_ProductionLoadBOM, sp_ProductionCreatePickTask, sp_ProductionFinish, sp_PickLinePost.
2. C2 idempotency: status guard (`IF @Status='POSTED' THROW 50010`) + StockMovement(SourceDocType,SourceDocId,...) UNIQUE — önce mevcut duplicate tara (CLI query).
3. Her SP sonrası sql-sp-reviewer + smoke (çift-post → 2. çağrı THROW).
