# Oturum Günlüğü — 2026-06-18

## Ana Konu
ERP customization/extension mimarisi tartışması (SAP ABAP / 1C / canias-IAS muadili) → Operax için "çekirdek üstüne kod geliştirme" katmanı. Sonuç: UDF (özel alan) feature'ı için Tier 3 plan yazıldı.

## Tamamlananlar
- Extension mimarisi spektrum analizi: SAP (compiled+hook) vs 1C/canias (platform+gömülü dil+configurator) iki felsefe ayrımı.
- Operax için en ucuz+performanslı öneri belirlendi: derlenmiş C# hook/BAdI + SQL-First (var) + UDF — **runtime assembly load YOK** (single-tenant gereği), Roslyn/configurator YOK. Derlenmiş C# → 1C/canias yorumlayıcısından hızlı.
- UDF tasarım belgesi (`docs/design/DYNAMIC_CUSTOM_FIELDS.md`) gerçek kodla karşılaştırıldı: **kodda 0 satır, sadece docs.** "Zaten var" sanısı düzeltildi.
- Tasarımdaki 6 açık tespit edildi (belge atlamış): tip-kayması/lexical-sort, sunucu validasyon eksiği, anahtar enjeksiyonu, DICTIONARY bağımlılığı, kültür/invariant, TABLE whitelist.
- code-architect (sonnet) blueprint üretti — gerçek pattern'lere oturtuldu (Items/Details.cshtml.cs, schema_M01.sql, Program.cs migrate listesi, UiVms.cs).
- `plans/34-udf-custom-fields.md` yazıldı — 4 faz, 6 açık baştan kapalı, 5-lens kontrolü dahil.

## Build / Test Durumu
- Build: çalıştırılmadı (kod değişmedi — sadece plan + journal dosyası).
- Test: çalıştırılmadı (kod değişmedi).
- Smoke: yok (implementation başlamadı).

## Commit Durumu
- Uncommitted dosya sayısı: 7 (6'sı önceki oturumlardan kalan + `plans/34-udf-custom-fields.md` yeni).
- Bugünkü commit: yok (bu oturumda kod yazılmadı).
- Commit beklemede: plan 34 (handoff ile commit edilecek). Diğer 6 dosya önceki oturum sorumluluğu — dokunulmadı.

## Aktif Planlar
- `plans/34-udf-custom-fields.md` — **YENİ, Taslak, onay bekliyor.** Faz 1 (Item, TEXT/NUMBER/SELECT_STATIC, ~9 dosya/~600 satır) onay sonrası başlar.
- `plans/33-sql-arch-conformance-fixes.md` — Faz A+B ✅ · Faz C/D/E/F kaldı (önceki oturumdan, dokunulmadı).

## Yarım Kalan / Yarın'a Bırakılan İşler
- **Plan 34 onayı** — kullanıcı onay/düzenle/council kararı vermedi; oturum handoff ile kesildi. Onay gelince Faz 1 adım 34-1 (schema_M_UDF.sql) ile başlanır.
- **Plan 33 Faz C-F** — hâlâ açık (8 SP transaction normalizasyon, DocumentLock helper, THROW kod hizalama, MovementDate simetri).

## İşe YARAMAYANLAR
- (yok — bu oturum salt tasarım/plan, başarısız deneme yok)

## Kararlar
- **Extension modeli:** 1C/canias tarzı platform+interpreter+configurator (Seviye 4-5) REDDEDİLDİ — solo ekip için yıllar, .NET/SQL-First mimarisine ters. Hedef: Seviye 0-1 (var) + 2 (hook) + opsiyonel UDF. Plan dosyasına işlendi (plan 34 §3).
- **UDF veri modeli:** JSON hibrit seçildi; EAV (JOIN maliyeti) ve gerçek-kolon (şema çatallanma) reddedildi. Plan 34 §3 Alternatifler.
- **6 açık baştan kapatılacak** — özellikle anahtar enjeksiyonu (whitelist) ve sunucu validasyon güvenlik kritik.

## Dikkat Edilmesi Gerekenler
- **`Items/Details.cshtml.cs:67-87`** halihazırda `Description` kolonunda JSON serialize/deserialize yapıyor (UDF-benzeri). Plan 34 Faz 1 implementasyonunda ilk dokunuşta bu okunmalı — `AdditionalFields`'a taşınırken çakışma/ayrıştırma riski.
- **Migrate kayıt yeri:** `src/Operax.Cli/Program.cs:~102` addon şema dizisi — yeni `schema_M_UDF.sql` buraya eklenmeli, yoksa migrate görmez.
- **DICTIONARY data source** Faz 1'de reddedilecek — `DictionaryType`/`DictionaryValue` tabloları henüz yok.

## Yarına Başlangıç Noktası
1. Kullanıcıdan plan 34 onayı al (onay/düzenle/council). Onaysa Faz 1 başlat.
2. Faz 1 ilk adım: `Items/Details.cshtml.cs:67-87` Description-JSON pattern'ini oku → `docs/sql/schema_M_UDF.sql` yaz (UserFieldDefinition + Item.AdditionalFields).
3. Alternatif: kullanıcı UDF yerine plan 33 Faz C'yi (SP transaction normalizasyon) tercih ederse oraya dön.
