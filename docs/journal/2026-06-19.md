# Oturum Günlüğü — 2026-06-19

## Ana Konu
Plan 34 UDF (Dinamik Kullanıcı Tanımlı Alanlar) — full pipeline uygulama: impl-spec → implementasyon (Faz 0+1) → phase-review-gate → browser smoke. (Oturum 18 Haz akşamı başladı, 19'a sarktı.)

## Tamamlananlar
- **impl-spec skill** yazıldı (`.claude/skills/impl-spec/SKILL.md`) — plandan dosya-dosya kod-temelli spec üreten disiplin (spec öncesi her dosyayı oku).
- **Plan 34 UDF Faz 0+1 implement edildi + commit'lendi** (branch `feat/udf-custom-fields`, 8 commit):
  - SQL: `schema_M_UDF.sql` (UserFieldDefinition + Item.AdditionalFields/MinStockLevel/MaxStockLevel + UpdatedAt/UpdatedBy), `db_objects_udf.sql` (Description-JSON backfill + Volume/Weight/TempRange seed), CLI migrate kaydı.
  - Backend: `UdfService.cs` (LoadDefinitions/ReadValues/BuildValidatedJson/GetStaticOptions), `UiVms.cs` (UdfFieldDef+CustomFieldsVm), DI.
  - UI: `_CustomFields.cshtml` panel, Items/Details (Description-JSON hack kaldırıldı), Items/Index (CriticalSku→SQL), Admin/UdfFields CRUD, _Layout sidebar.
- **phase-review-gate:** build 0/0 (Web+CLI) · code-reviewer (1 HIGH düzeltildi) · sql-sp-reviewer (2 CRITICAL backfill veri-kaybı → COALESCE+NULLIF düzeltildi) · security (temiz) · crafted-row smoke (idempotent, veri kaybı yok).
- **Browser smoke (preview, login→ekran):** Admin 3 seed render · Item UDF paneli render · kaydet→DB→reload round-trip (`{"Volume":"42","TempRange":"Soğuk"}`) · Admin'den yeni alan→Item formunda otomatik render = tam dinamik döngü ✅.

## Build / Test Durumu
- Build: **yeşil** (Web+CLI 0 hata; 1 pre-existing ForwardedHeaders uyarısı plan dışı).
- Test: otomatik test yok (proje durumu).
- Smoke: **yapıldı** — SQL crafted-row + browser uçtan uca, ikisi de geçti.

## Commit Durumu
- Uncommitted: 7 (6'sı pre-existing önceki oturum + `docs/sql/smoke_udf.sql` test artefaktı).
- Bugünkü (gece yarısı sonrası) commit: yok — UDF commit'leri 18 Haz gecesi atıldı (a156438→7de999d, 8 adet).
- **smoke_udf.sql** silinmeli (rm izni reddedildiği için diskte kaldı, untracked — commit edilmedi).

## Aktif Planlar
- `plans/34-udf-custom-fields.md` — **Faz 0+1 TAMAMLANDI** (Onaylandı, uygulandı, smoke geçti). Faz 2 (DATE/BOOLEAN, computed-index, DICTIONARY/TABLE source, Partner entity), Faz 3 (inheritance), Faz 4 kaldı.
- `plans/33-sql-arch-conformance-fixes.md` — Faz C/D/E/F hâlâ açık (dokunulmadı).

## Yarım Kalan / Yarın'a Bırakılan İşler
- **UDF tanım düzenleme eksik (kullanıcı sordu):** Admin Kaydet sadece LabelText/OrderNo/IsRequired/IsActive güncelliyor. **DataSourceKey (SELECT seçenek listesi) düzenlenemiyor** → renge yeni renk eklemek için sil+yeniden aç gerekiyor. Tier 2 ek (~2 dosya): `OnPostSaveAsync`'e DataSourceKey param + view'a input. FieldName/FieldType kilitli kalmalı (veri bütünlüğü). **Kullanıcı bunu istedi — yarın ilk iş aday.**
- "Renk" alanı: kullanıcı seçilebilir istiyor → SELECT/STATIC ile bugün mümkün (TempRange gibi); merkezi sözlük isterse DICTIONARY = Faz 2.

## İşe YARAMAYANLAR
- **preview_fill + preview_click ile login** → form submit POST'u oluşmadı (button click form'u göndermedi) + araya navigate edince session düştü. **Çözüm:** eval ile `form.submit()` (anti-forgery hidden field dahil) + redirect'i `sleep` ile bekle. Yarın browser smoke'ta direkt bu yöntem.

## Kararlar
- **UDF depolama:** ayrı `AdditionalFields` kolonu (Description-JSON hack temizlendi). Volume/Weight/TempRange → seed UDF (hardcode kolon değil). MinQty/MaxQty → gerçek kolon (CriticalSku KPI besliyor). Plan 34 §3/§9.5.
- **impl-spec disiplini kalıcılaştı** — plandan sonra dosya-dosya spec, kod okumadan yazma yok.

## Dikkat Edilmesi Gerekenler
- **2 PRE-EXISTING bug smoke'ta çıktı, düzeltildi (commit a9e3cf0):** (1) Items/Details formunda hidden `Item.Id` yoktu → INSERT dup-key; (2) Item'da `UpdatedAt/UpdatedBy` kolonu yoktu → UPDATE "Invalid column". **İkisi de `main`'de de var → ürün düzenleme proje-genelinde kırıkmış.** Salt-okuma planlama bulamazdı; çalıştırınca çıktı. → diğer evrak edit ekranları da aynı hidden-Id/UpdatedAt eksikliğinden muzdarip olabilir, kontrol edilmeli (DEBT).
- HAM-001 (demo ürün) üzerinde smoke UDF değerleri kaldı (Volume=42, TempRange=Soğuk, Description="Smoke açıklama") — zararsız demo verisi.
- `migration_add_updatedby.sql` migrate listesinde DEĞİL (vestigial) — UpdatedBy artık schema_M_UDF.sql'de.

## Yarına Başlangıç Noktası (DETAYLI — kendi-kendine yeter)

### Hızlı bağlam (her şeyden önce)
- **Branch:** `feat/udf-custom-fields` (main DEĞİL). 13 commit: UDF Faz0+1 + 2 edit-bug fix + çatı adaptasyonu. Build Web+CLI **0/0**.
- **Çalışan dev server:** kapalı. Açmak: preview `launch.json` adı **`Operax.Web`**, port **5193**. Login redirect → `/login`.
- **Test giriş:** `admin@operax.com` / `Admin123!` · Şirket: "Operax Demo LTD" = **d1e1b1a5-...**. (Smoke test şirketi 00000000-... AYRI, stok verisi onda.)
- **Yeni araçlar (bu oturumda eklendi):** `/yetenek-uret` skill (çatıya skill/agent/hook/rule üret), `footprint-ladder.md` rule (yeni yetenek en-dar-basamak), `impl-spec` skill (plandan dosya-dosya spec).

### İş 1 — UDF DataSourceKey (seçenek listesi) düzenleme [kullanıcı istedi, Tier 2]
**Sorun:** Admin/UdfFields Kaydet (`OnPostSaveAsync`) sadece LabelText/OrderNo/IsRequired/IsActive güncelliyor; **DataSourceKey (SELECT seçenekleri) düzenlenemiyor** → renge yeni renk eklemek için sil+yeniden aç gerekiyor.
**Dosyalar + ne yapılacak:**
- `src/Operax.Web/Features/Admin/UdfFields/Index.cshtml.cs` → `OnPostSaveAsync` imzasına `string? dataSourceKey` ekle; UPDATE SET'e `DataSourceKey=@DataSourceKey` ekle (yalnız FieldType='SELECT' satırlarda anlamlı).
- `src/Operax.Web/Features/Admin/UdfFields/Index.cshtml` → tablodaki Kaydet (`asp-page-handler="Save"`) satır-form'una seçenek input'u ekle (mevcut DataSourceKey value'lu).
**KİLİTLİ kalmalı (DEĞİŞTİRME):** `FieldName` (JSON anahtarı — değişirse kayıtlı veri yetim kalır), `FieldType` (TEXT↔NUMBER mevcut veriyle uyumsuz). Sadece DataSourceKey + zaten-açık 4 alan.
**Test:** Admin'de bir SELECT alanın seçeneklerine renk ekle → Item Details'ta dropdown'da çıkmalı.

### İş 2 — Edit-bug süpürmesi [smoke'ta çıkan pattern, muhtemelen proje-geneli]
**Bulgu (bugün):** Items/Details edit-save İKİ pre-existing bug'la kırıkmış: (1) formda hidden `Item.Id` yoktu → INSERT dup-key; (2) Item'da `UpdatedAt/UpdatedBy` kolonu yoktu → UPDATE "Invalid column". İkisi de `main`'de de var.
**Yapılacak:** Diğer master/evrak Details edit ekranları AYNI bug'dan muzdarip mi tara:
- `grep -rn 'asp-for="<Entity>.Id"' src/Operax.Web/Features/` → hidden Id eksik olan edit formları bul.
- Her edit-UPDATE'i çalıştıran tablo `UpdatedAt/UpdatedBy` kolonuna sahip mi? (Partner, PurchaseOrderHeader, SalesOrderHeader… kontrol et — `migration_add_updatedby.sql` migrate listesinde DEĞİL, vestigial).
- Aday ekranlar: `MasterData/Partners/Details`, `MasterData/Branches/Details`, PO/SO Details. Her birinde hidden Id + UpdatedAt kolon kontrolü.
**Not:** Gerekirse toplu fix + Tier 2 plan; çok dosyaysa Tier 3.

### İş 3 — Temizlik + merge
- `docs/sql/smoke_udf.sql` SİL (bu oturumda rm izni reddedildi; sabah dene veya editörden sil). Untracked, commit edilmedi.
- HAM-001 (demo ürün) üzerinde smoke UDF değerleri var (Volume=42, TempRange=Soğuk, Desc="Smoke açıklama") — zararsız, istenirse temizlenir.
- `feat/udf-custom-fields` → merge/PR kararı (build 0/0, review+smoke geçti).

### İş 4 (opsiyonel) — sıradaki büyük iş
- Plan 34 **Faz 2**: DATE/BOOLEAN alan tipi + computed-index + DICTIONARY/TABLE data source + Partner entity (plan §7).
- VEYA Plan 33 **Faz C** (askıda): 8 SP transaction normalizasyon (TRY/CATCH+ROLLBACK+THROW) + idempotency.

---

## Ek Tur — Çatı Adaptasyonu (pusula → Operax)

**Konu:** `D:\Dev\pusula\.claude` skill/agent/hook/rule/script envanteri tarandı; Operax'a adapte edilebilecekler uyarlanarak alındı.

**Alındı (commit 6924693 + 06b9bd0):**
- `footprint-ladder.md` (YENİ rule) — yeni yetenek en-dar-basamakta (skill<rule<agent<SP/şema<Razor sayfa); Operax stack'ine uyarlandı.
- `yetenek-uret` (YENİ meta-skill) — çatıya disiplinli skill/agent/hook/rule üretir/günceller (footprint-ladder + convention + kayıt + test + onay).
- Aynı-isim dosyalarda pusula'da daha iyi + Operax'ta eksik 3 jenerik kazanım: **commit-discipline** (commit→TODO `[x]` senkron + zararlı git komut onay listesi), **session-protocol** (SessionStart hook'u koşulsuz tekrar çalıştır — context'e güvenme dersi), **agent-usage** (leaf/orchestrator rol + derinlik≤2 + max_concurrent=3).

**Alınmadı (gerekçeli):** BKM-domain skill'ler (sema/dashboard/gm-rapor/forecast/denetim), `consolidate-sema`/`sema-*` (cross-project-sema + global consolidate-memory kapsıyor), `file-size-discipline` (csharp-conventions'da var), `python-reviewer` (C# stack), `sql-denetci` (sql-sp-reviewer var), hook'lar (Operax-tuned, churn riski), `session-handoff`/`plan-tracker` skill'leri (pusula'da büyük ama BKM-tuned — sema-sync + bkm/ subdir; Operax'ınki sade+çalışıyor → footprint-ladder gereği bozulmadı), PowerShell script'ler (pusula-makine altyapısı).

**Karar:** Aynı isim ≠ aynı kalite — içerik bazında diff edildi; Operax'ta zaten daha zengin olanlar (error-handling 130>50, silent-failure 126>27, test/security/turkish-ui) değiştirilmedi. Yalnız net üstün + eksik olanlar cerrahi eklendi.

---

## Ek Tur 2 — Plan 33 C-F + extensibility + fresh-install (devam)

**Branch:** `feat/plan33-fazc-sp-transaction` (UDF merge'i main'e gitti 3330704; sonrası bu branch'te birikti). Build Web 0 hata.

### Plan 33 — Faz C/D/E/F TAMAMI bitti (her biri review+smoke+commit)
- **Faz C** (a6c4216): ProductionFinish + PickLinePost idempotency (UPDLOCK+status guard+TRY/CATCH). Doğrulama plan'ı 8→2 SP'ye indirdi (ShippingPost zaten hardened-starter; UNIQUE-index fikri çok-satır nedeniyle çalışmaz → status guard doğru). Smoke: COMPLETED emre finish→THROW 50010.
- **Faz D** (3e15dd2): `Lib/DocumentLock.cs` (4 async helper) + PO/SO/Shipping/Receiving edit+addline guard. code-reviewer 2 fix: ReceivingHasInvoice ölü tabloya (ExpenseInvoice.ReceivingId=0-dolu) bakıyordu → canlı bağ PurchaseInvoiceLine.SourceReceivingLineId (rule §7 stale); Receiving edit guard eksikti.
- **Faz E** (57c8877): THROW 60xxx→55xxx (iş-hatası bandı dışıydı→generic mesaj) + 2 DEAD servis sil (ProductionReceipt/ActivityService).
- **Faz F** (83d7a5a): StockMovement.MovementDate + trigger INSERTED.MovementDate (AccountMovement simetrik) + 189 backfill. Smoke: kapalı 2020/3 döneme MovementDate'li SM→THROW 51210. C1-5 polish ERTELENDİ (XACT_ABORT mitigates).

### Yeni yetenekler (framework)
- `impl-spec` skill (önceki tur) · `demo-veri-uret` skill + `demo-data-builder` agent · `referans-tanim-seed` skill · `pre-commit-antipattern` CLAUDE_PRECOMMIT_SKIP toggle.

### Fresh-install migration FIX (E2E fresh-DB ile yakalandı — BUGS FBUG-1/2/3 ÇÖZÜLDÜ)
Operax_Test DB'de iteratif `migrate` → **0 fail** (100 tablo). Düzeltmeler: migration_add_updatedby wire + ItemUnit OBJECT_ID guard + ReceivingHeader/ShippingHeader UpdatedBy/At · schema_M05_DocChain ALTER sonrası GO (batch-compile) · Item.Description StarterFields'e. **Kalan FBUG-4:** statik seed (seed_demo/dashboard/finance) FK-kırık (hardcoded GUID, fresh master yok) — DEMO veri, baseline değil.

### Referans veri + extensibility (derin araştırma)
- `REFERENCE_INVENTORY.md` — kod+rakip+mevzuat envanter; web-sertleştirme **3 üretim-kritik hata** yakaladı: tevkifat T10 uydurma→gerçek GİB 601-625, kitap KDV %10→%1, Adet UN/ECE NIU→C62.
- **ADR-02** — referans veri hibrit: mantık-dallandıran CODE kod-çıpalı (status/movement/source/cheque — SP bozulmasın), gerisi dict-dinamik+UDF.
- **CUSTOMIZATION_EXTENSIBILITY.md** — SAP/1C/Odoo/BC/Frappe müşteri-özel-ekran araştırması → Operax merdiveni (UDF→SP→named-slot→C# hook→custom sayfa). En değerli boşluk: `IDocumentHook` (Before/AfterPost, single-tenant→runtime-load YOK; ledger SP'de kalır, hook=yalın yan-etki).

### pusula re-scan + hook
- Fresh tam tarama: uygulanabilir net-yeni öğe YOK (applicable set ilk turda alındı). pusula sql-server-conventions tamamen BKM-domain; 4 hook Operax'ta wire'lı+canlı (pusula'nınkiler bkm/ subdir yazıyor — Operax'ınki daha zengin).
- Hook event-kapsam: SessionStart/PreToolUse/PostToolUse/PreCompact ✅ wire'lı. Eklenebilir: A·PostToolUse(Edit/Write) warn, B·SessionEnd — kullanıcı kararı bekliyor.

## Build / Test (Ek Tur 2)
- Build: **Web 0 hata** · CLI 0 hata. Fresh migrate 0 fail (Operax_Test).
- Smoke: Faz C (THROW 50010) · Faz F (THROW 51210) · DocumentLock (faturalı receiving→guard true) · fresh-DB migrate. Hepsi geçti.

## Yarına Başlangıç (Ek Tur 2)
1. **Branch çok dolu** — `feat/plan33-fazc-sp-transaction` scope-mixed (plan33 C-F + 4 framework yeteneği + migrate fix + 3 referans/design doc). Ayrı PR'lara bölme veya merge kararı.
2. **P0 `seed_reference.sql`** üret (REFERENCE_INVENTORY P0: UOM+UnEceCode+C62, TAX her şirkete, CURRENCY/PAYMENT dict, WITHHOLDING 601-625) — referans-tanim-seed skill ile.
3. **B1 `IDocumentHook`** ayrı Tier-3 plan (extensibility — en değerli pro-code katmanı).
4. FBUG-4 statik seed FK-kırık temizliği (ayrı). C1-5 SP TRY/CATCH polish (düşük).
5. Hook A/B eklensin mi kararı.

## Dikkat (Ek Tur 2)
- `docs/sql/smoke_udf.sql` hâlâ untracked (rm izni reddediliyor — editörden sil).
- HAM-001 + demo şirket 00000000'da smoke UDF/akış verisi kaldı (zararsız).
- Fresh-install için tam seed henüz operable değil (FBUG-4 + seed_reference üretilmedi); demo-data-builder fresh DB'de warehouse/bin de kurmalı (Warehouse=0).
- 11 uncommitted: çoğu pre-existing (before-major-change/session-handoff M, .agents/.codex/AGENTS untracked, planner/learn/cross-project-sema, smoke_udf).

### Ek — Hook genişletme (oturum sonu)
- `pre-commit-antipattern.sh` → `CLAUDE_PRECOMMIT_SKIP=1` toggle (pusula generic uyarlama).
- **YENİ hook A:** `post-edit-antipattern.sh` (PostToolUse `Edit|Write|MultiEdit`) — dosya yazınca anlık antipattern UYARI (warn-only, exit 0; commit-gate bloklayan kapı). `CLAUDE_POSTEDIT_SKIP=1` toggle. settings.json wire + test (temiz→exit0, inline-style→warn).
- Hook seti artık **5 aktif:** SessionStart · PreToolUse(commit-gate) · PostToolUse(Bash→journal + Edit/Write→anlık-warn) · PreCompact.
- Hook B (SessionEnd) eklenmedi — kullanıcı A'yı seçti.
- pusula re-scan kesin sonuç: uygulanabilir net-yeni öğe yok (sql-server-conventions BKM-domain; 4 ortak hook Operax'ta daha zengin/wire'lı).
