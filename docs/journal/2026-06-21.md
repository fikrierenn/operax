# Oturum Günlüğü — 2026-06-21

## Ana Konu
PR #1 akışı + Plan 35 (baseline referans seed) kapanışı + Codex PR review fix'leri + fresh-install bug'ları + Plan 37 Faz 3 (DB integration harness + ledger testleri) + dönem-kilidi guard fix'i.

## Tamamlananlar
- **PR #1** oluşturuldu/güncellendi (gh auth login sonrası). Codex review 4 bulgu kapatıldı + thread'ler reply+resolve.
- **Codex review fix (1 P1 + 3 P2):** Users/Create+Edit UserCompany MERGE upsert (rol login'de çözülüyordu, yeni Admin kilitleniyordu) · Production/Terminal çapraz-firma aktivite kapatma → ProductionOrder JOIN+CompanyId · PurchaseInvoices/Details AI timeout soft-fail → UNCHECKED · Program.cs ForwardedHeaders loopback'e kısıt (XFF spoof rate-limit bypass).
- **Plan 35 (4 faz, arşivlendi):** schema_M00_DictRefCols (UnEce+IsWholeNumber) · seed_reference.sql 9 baseline tip per-company (sql-sp-reviewer CRIT-1 FK-repoint + XACT_ABORT) · Dtos AccountType/TransactionType/PaymentMethod + Finance refactor · fresh Operax_Test doğrulama.
- **Fresh-install fix:** migration_set_compat.sql (compat 160 sigortası, sürüm-korumalı) · seed_demo_master.sql (demo master eksikti, sabit-GUID) · demo seed'ler default `seed`'den ayrıldı → yeni `seed-demo` komutu.
- **Plan 37 Faz 3a-d:** DatabaseFixture (izole Operax_Test, conn-swap, operax-cli migrate+seed) · 9 integration test: harness/baseline-regresyon/posting=100/çift-post-THROW/reversal-net0/CompanyId-izolasyon/dönem-lock-51201.
- **Dönem-kilidi fix (ledger §1.b):** 7 ledger SP'sine sp_GuardPeriodOpen eklendi (Receiving/Shipping/PickLine/PayCreditCard/CreateLoan/PayLoanInstallment/PurchaseInvoiceReverse). sql-sp-reviewer IMP-1 (reverse guard belge tarihiyle) düzeltildi.

## Build / Test Durumu
- Build: CLI yeşil (0/0). Web: derleme temiz (0 hata; final adımda app çalıştığı için bin kilitli, temp-output 0/0 doğrulandı).
- Test: **29/29 yeşil** (20 mevcut birim + 9 yeni integration).
- Smoke: SP fix'leri dev Operax'a deploy + Operax_Test fresh migrate ile uçtan uca doğrulandı.

## Commit Durumu
- Uncommitted dosya sayısı: 0
- Bugünkü commit'ler: 7 (PR-review fix → Plan35 kapanış → fresh-install fix → Faz 3a/3b/3c → roadmap notu → ledger guard fix)
- **Unpushed: 4** (Faz 3b, 3c, roadmap, ledger-fix) — push harness'ta deny kuralında, kullanıcı elle pushluyor.

## Aktif Planlar
- **plans/37-test-infrastructure.md** — Faz 1/2/3a/3b/3c/3d ✅. Kalan: Faz 3e (UOM dönüşüm/fiyat-vergi testi) + Faz 4 (auth/role/company-switch).
- Plan 33 + Plan 35 bu/önceki oturumda arşivlendi.

## Yarım Kalan / Yarın'a Bırakılan İşler
- **Plan 37 Faz 3e:** fn_GetConversionRate dönüşüm + sp_CheckPriceVariance testi.
- **Plan 37 Faz 4:** DapperUserStore concurrency, UserCompany IsActive guard, fallback authz testleri.
- **B3 Satınalma öneri-sipariş** (roadmap Backlog): tvf_ReplenishmentSuggestions verisi hazır → ekran+draft-PO action; düşük footprint, ileride plan.
- **INFO borcu:** finans SP'lerinde @UserId UNIQUEIDENTIFIER→NVARCHAR conversion, CLOSED-dönem override smoke ile doğrulanmalı.

## İşe YARAMAYANLAR
- İlk dönem-guard taramam (awk range + `\b` + CRLF) yanlış sonuç verdi ("tüm post SP guard'sız") → otoriter awk taraması (CREATE…GO blok + ledger-INSERT tespiti) gerçeği gösterdi: Transfer/CycleCount/Production zaten guard'lı, sadece 7 SP eksik. **Ders:** SP-guard iddiasını blok-bazlı tara, satır-grep'e güvenme.
- İlk migration_set_compat `ALTER…160` koşulsuzdu → SQL 2019 localdb'de "valid values 100-150" hatası. Sürüm-korumalı (ProductMajorVersion>=16) + tolerant yapıldı.
- PurchaseInvoiceReverse guard'ını GETUTCDATE ile koymak → sql-sp-reviewer dönem-boşluğu yakaladı (ters hareket @SupInvDate'e yazılıyor) → belge tarihiyle hizalandı.

## Kararlar
- Default `seed` = sadece baseline (operable, sahte veri yok); demo opt-in `seed-demo` komutu (single-tenant + UI-standard §1.5 ile uyumlu).
- DB integration test stratejisi: izole `Operax_Test` katalog (aynı sunucu, conn-swap), operax-cli migrate+seed (tek-kaynak sıra, drift yok).
- Compat 160 sigortası migrate ilk adımı (hedef SQL 2022, model<160 nadir senaryoya karşı).

## Dikkat Edilmesi Gerekenler
- **Test ortamı = SQL 2019 localdb (model 150)**; gerçek Operax = BT-FIKRI\SQLEXPRESS (2022, model 160). STRING_SPLIT ordinal yalnız 2019'da patlar; fresh-install harness Operax_Test'i 2022 sunucusunda kuruyor (tam migrate).
- **Push harness'ta deny** — `.claude/settings` permissions. Kullanıcı elle pushluyor; commit'ler birikiyor.
- Spawn task'lar: task_25b84bb3 (dönem-guard, bu oturumda yapıldı) · task_c6b83089 (fresh-install, bu oturumda yapıldı).

## Yarına Başlangıç Noktası
1. **`git push`** (kullanıcı) — 4 commit unpushed, PR #1 güncellensin → Codex re-review.
2. **Plan 37 Faz 3e** — fn_GetConversionRate + sp_CheckPriceVariance integration testi (harness + SetupDraftReceiving helper hazır).
3. **INFO smoke** — CLOSED dönem + Finance rolü + gerçek @UserId ile finans ödemesi → override log doğrula (@UserId conversion).
