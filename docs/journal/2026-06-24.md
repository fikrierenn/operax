# Oturum Günlüğü — 2026-06-24

## Ana Konu
Tasarım sistemi standardını kurmak ve **modül modül** uygulamak; ardından M0'dan başlayarak her modülde **hem kod revizyonu hem tasarım** standardizasyonu. (Oturum Mosaic anasayfa denemesiyle başladı, tam tasarım-sistemi programına dönüştü.)

## Tamamlananlar
- **Mosaic anasayfa → aktif Dashboard:** sparkline (anlam-renkli) + delta pill sales kartı + mini-KPI şeridi; utility-soup → semantic refactor; eski gradient-KPI anasayfa + `Mosaic.cshtml/.cs` silindi. (`3dfc1ca`, `dda634f`, `cd676b9`)
- **Tasarım sistemi (Plan 53 Faz A+B):** `_stat.css` (`.stat-card`/`.stat-spark`/`.delta-pill`/`.stat-bar`, CSS-driven sparkline renk) + `--radius`/`--overlay` token + ui-standard §4.5/§4.6 (kanonik bileşen kataloğu). Dashboard semantic'e çekildi. (`cd676b9`, `db5faa5`)
- **2 stale CRIT kapatıldı** (koddan doğrulandı, zaten fix'liymiş): CRIT-1 SP THROW catch + CRIT-2 XSS _PageHeader. (`2ea3caa`)
- **Bug fix:** `Db.Open()` bağlantıyı açmıyordu → `BeginTransaction` "Bağlantı kapalı". Eager-open. 5 latent-kırık handler düzeldi (Expenses AddLine uçtan-uca doğrulandı). (`d76a814`)
- **Alpine→vanilla (CSP):** Sözlük Değerleri satır-içi edit Alpine x-show CSP'de sessiz kırıktı → vanilla toggle; razor-conventions JS standardı **vanilla-first** revize. (`6015ba5`, `4fec840`)
- **Tasarım modülleri TAMAM:** MasterData (Items yapısal stat-card grid+form-ctrl, Locations rebuild, PriceLists×3, Branches×2, Partners) + Warehouses (Details rebuild + Index). (`5f80209`, `c71ff7c`)
- **Tam ekran denetimi:** 6 paralel ajan ~89 ekranı taradı → 6 sapma tipi + kritik bulgu + worst-first modül rollout (plan 53'e işlendi). (`db5faa5`)
- **M0 Platform Core kombine revizyon (3 batch):** `"Administrator"` magic string→sabit (13 dosya); Türkçe metot yorumu (Auth/Login, Roles ×4); Users/Create validation; tasarım: Roles/Create, Modules, StatusTransitions, Parameters → semantic. (`53085fc`, `585e7cf`, `f4b01eb`)
- **Veri fix:** UOM sözlük tipi DB'de mojibake (`Ã–lÃ§Ã¼`) → `Ölçü Birimi` (stale dev verisi; kaynak temiz, sadece DB UPDATE).

## Build / Test Durumu
- Build: **yeşil (0/0)** — her batch'te doğrulandı, son build 0 hata.
- Test: çalıştırılmadı (UI/tasarım işi; otomatik test kapsamı dışı).
- Smoke: tarayıcıda screenshot + computed-style ile her ekran doğrulandı. **Not:** preview_screenshot oturum ortasında bir süre wedged kaldı (getComputedStyle ile doğrulandı), sonra kurtuldu.

## Commit Durumu
- Uncommitted: 1 (`docs/journal/2026-06-23.md` — session-start snapshot churn; bu commit'te kapanır).
- Bu oturum ~20 commit (Mosaic + tasarım sistemi + MasterData/Warehouses + denetim + M0 ×3).

## Aktif Planlar
- `plans/53-design-system-standard.md` — **AKTİF.** Faz A+B ✅ · Faz C başladı: MasterData ✅, Warehouses ✅, Dashboard ✅, **M0 ✅**. Tam ekran denetimi + worst-first modül rollout sırası içeride.

## Yarım Kalan / Yarın'a Bırakılan İşler
- **M0 borcu (düşük öncelik):** Users/Edit IDOR (`Input.Id`→route+`[BindNever]`, Administrator-only=düşük risk) · Dashboard `MovementType` magic string ('RECEIPT'/'ISSUE' — salt-okuma display CASE).
- **Sıradaki modül = M1 Manufacturing** (BOM/WorkCenters/WorkOrders ×5): util-renk AĞIR (~74/ekran) + `class="input"/"label"`→`.form-ctrl`/`.form-label` + bespoke help-panel (`bg-amber-50`).
- **Worst-first rollout (plan 53):** M1 Manufacturing → WMS-ops (Transfer/Picking bespoke buton+panel+header) → Belgeler (PurchaseInvoices/Expenses/MaterialIssue) → Finance → kalan Admin → Misc (Lot/LPN/Serial/Budget).
- **🔴 KRİTİK (Belgeler modülünde):** Expenses/Index + MaterialIssue/Index badge'leri İngilizce enum gösteriyor (Draft/Posted/Paid…) → `Dict.StatusBadge()` Türkçe helper'a çevrilmeli.

## İşe YARAMAYANLAR
- **Alpine.js standard build** → CSP `unsafe-eval` yok, `x-data`/`x-show` sessizce kırık (konsol hatası bile yok). Vanilla'ya geçildi; kural vanilla-first yapıldı. Yeni `x-` attribute YAZMA.
- **preview_screenshot** oturum ortasında wedged (her sayfada timeout) → DOM `getComputedStyle` ile renk-doğrulama yapıldı (token değeri çözülüyor mu); sonra kurtuldu.
- **`operax-cli` PATH'te yok** → `dotnet run --project src/Operax.Cli -- query "..."` kullan.
- **dotnet build exe-kilidi** (MSB3027) → preview server çalışırken build "2 hata" verir ama kod hatası değil; build öncesi `preview_stop`.

## Kararlar
- **Standart = mevcut semantic sistem (ui-standard §4.6), Mosaic görsel dili oraya taşındı.** Utility-soup'u 102 ekrana YAYMA — renk/bileşen token+semantic, yerleşim Tailwind utility serbest. (kullanıcı: "her bileşen tipi standart olmalı")
- **Yapısal standardizasyon (renk yetmez):** ekranlar Dashboard Mosaic diline yapısal çekilir (stat-card grid + .card + form-ctrl), sadece renk-token değil. (kullanıcı: "items kpi aynı değil")
- **UI %100 Türkçe:** UOM→Birim, Zone→Bölge, Staging→Hazırlık (kod identifier `bin.Zone` hariç). (kullanıcı: "arayüzde ingilizce yoktu")
- **Program: M0'dan başlayarak modül modül kod revizyonu + tasarım** (kullanıcı kararı). Her modül: code-reviewer + (gerekirse security/sql-sp) → kod fix → tasarım standardı → build+screenshot → commit.

## Dikkat Edilmesi Gerekenler
- code-reviewer (haiku) bazı yorum-eksikliği bulgularını **false-positive** verdi (Logout/Users/Index'te yorum aslında vardı) — uygulamadan önce file:line doğrula.
- `.card` radius `--radius` token'a bağlandı (değer 14px sabit) → 60 .card ekranı görsel değişmedi.
- Sparkline rengi CSS modifier (`is-success/warn/danger`) ile — inline `fill`/`stroke` YOK (CSP + tema).
- Modül haritası (Modules ekranından doğrulandı): M00 Platform Çekirdek · M01 Ana Veri · M02 Envanter · M03 Satınalma · M04 Satış Siparişi · M05 Sevkiyat · M06 Picking · M07 Transfer…

## Yarına Başlangıç Noktası
1. **M1 Manufacturing modülü:** BOM/WorkCenters/WorkOrders ×5 — color-sed (token) + `class="input"/"label"`→`.form-ctrl`/`.form-label` + help-panel semantic. code-reviewer (Manufacturing PageModels) paralel. Build + screenshot + modül batch commit.
2. M0 borcunu kapat (Users/Edit IDOR + Dashboard MovementType) — istersen M1 öncesi hızlı.
3. Sonra WMS-ops modülü (en çok bespoke buton/panel).

## EK NOT (geç bulundu) — 2026-06-23 öğleden sonra KAYIP OTURUM
Bu konuşmada bağlamda olmayan ~12:48–20:42 oturumu commit'lerden geri-kurtarıldı ve `docs/journal/2026-06-23.md` "OTURUM 3" bölümüne işlendi: Plan 50/51/52 (item taksonomi iki-eksen, zayiat KDV dictionary-driven, M2 taslak), M18 gider faturası (vade auto-fill + Expenses/Details altın-prototip), muhasebe-mevzuat skill, mobil-responsive app-shell + Tailwind-layout-serbest kuralı (akşam tasarım-sistemi işinin temeli).

---

## EK (devir sonrası, aynı oturum) — kayıp oturum kurtarma + açık-iş konsolidasyonu

İlk devir özetinden (`138bfec`) sonra kullanıcı uyardı: "kaybolan bir session oldu, bugün daha çok şey yaptık" + "kalan işleri listelere işle".

### Yapılanlar
- **Kayıp oturum geri-kurtarıldı** (`d24c258`): 2026-06-23 öğleden sonra (~12:48–20:42) oturumu bu konuşmada bağlamda yoktu; commit'lerden reconstruct edilip `docs/journal/2026-06-23.md` "OTURUM 3" bölümüne işlendi. İçerik: Plan 50/51/52 (item taksonomi iki-eksen, zayiat KDV dictionary-driven, M2 taslak), M18 gider faturası (vade auto-fill + combobox + Expenses/Details altın-prototip), muhasebe-mevzuat skill, mobil-responsive app-shell + Tailwind-layout-serbest kuralı.
- **Açık işler listelere işlendi** (`1a1ca6f`): TODO başına taze `2026-06-25` öncelik bloğu (A: modül rollout tasarım+kod · B: Plan 50 M2 kod tarafı 4 faz · C: debt kalıntıları). Plan 50 durumu **AKTİF** + plan 53 çapraz-referans (tasarım=53, kod=50 ayrımı netleşti).

### Sonuç
- Çalışma alanı temiz (0 uncommitted). Build son durum 0/0 (devir sonrası yalnız docs değişti, kod dokunulmadı).
- **Yarın tek bakış noktası: `docs/TODO.md` ⭐⭐ 2026-06-25 bloğu.**

---

## EK 2 (devir sonrası) — kural eklemeleri (/learn + danışman-skill kuralları)

İlk ek'ten (`19473be`) sonra kural sertleştirmeleri yapıldı:
- **`/learn` dersleri** (`9b189f7`): `csharp-conventions` Dapper → `db.Open()` eager-açık, `BeginTransaction` öncesi conn açık olmalı (2026-06-24 Db.Open bug'ı). `test-discipline` Workflow → build öncesi `preview_stop` (MSB3027 = exe-kilidi, kod hatası değil; .cshtml/CSS recompile için restart).
- **Finans danışma kuralı** (`4624055`): `coding-discipline §5` — finans/muhasebe/mali-evrak kodu öncesi muhasebe-mevzuat / mali-evrak-mevzuat / mali-islem-akislari skill'e danış (ZORUNLU).
- **Ekran danışma kuralı** (`228fc1d`): `ui-standard §8` — ekran oluştur/revize öncesi screen-ux-standard / ux-design-patterns / tailwind-responsive skill'e danış.
- **Master danışman katalog** (`246428f`): yeni `advisor-skills.md` — TÜM danışman skill'leri tek tabloda (iş türü→skill, zorunlu kod-öncesi danışma) + eylem skill'leri ayrıldı + erp-isleyis-danismani ajanı modelleme için.

### Yakın oturumda üretilen skill'ler (hatırlatma)
muhasebe-mevzuat · mali-islem-akislari · tailwind-responsive (yeni) + screen-ux-standard (genişletildi) — hepsi 2026-06-23.

### Durum
Çalışma alanı temiz (0 uncommitted). Build 0/0 (devir sonrası yalnız `.claude/rules/` + journal docs). **Yarın başlangıç: TODO ⭐⭐ 2026-06-25 bloğu — M1 Manufacturing (danışman skill'lere danışarak).**
